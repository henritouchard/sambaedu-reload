# Code Review — Story 40.2 : Domaine Guide « Machines » (catégorie `computer`)

Status: to-validate
Date: 2026-07-07
Dev model: claude-opus-4-8[1m]
Review model: claude-sonnet-4-6 (adversariale)
Second review: oui (claude-opus-4-8[1m])

## Questions en attente

> Une seule décision produit, **non-bloquante** (#4) : conditionner ou non le lien how-to « Machines » à `computer.view`. Impact strictement limité à un profil custom mal formé ; aucun rôle livré concerné. Laissé en l'état + documenté comme limite connue (runbook QA). À trancher plus tard si des profils custom « control/RDP sans view » apparaissent.

## Synthèse des problèmes

| # | Problème | Sonnet | Pertinence Opus | Statut |
|---|----------|--------|-----------------|--------|
| 1 | Test hub 40.1 ne ré-assère pas `domain-computer` cliquable après 40.2 | 🔴 | 1 (surévalué) | ✅ Durci (assertion positive ajoutée) — la couverture existait déjà dans `GuideMachinesTest` |
| 2 | `featureCount()` compterait le total, commentaire « déverrouillées » trompeur | 🟠 | 0 (invalide) | ❌ Écarté — le commentaire dit « rendues », sémantique correcte |
| 3 | `assertSee('2 / 5 accessibles')` non scopé au testid | 🟠 | 1 | ❌ Laissé — chaîne unique en contexte (seul `computer` a total 5) |
| 4 | Lien `app.parc.index` exige `computer.view` → 403 possible pour rôle custom `control`-sans-`view` | 🟠 | 2 | ⏳ Arbitrage produit (documenté comme limite connue) |
| 5 | `null['key'] ?? ''` deprecation PHP 8 / TypeError PHP 9 | 🟡 | 0 (invalide) | ❌ Écarté — `??` a la sémantique `isset()`, aucun warning |
| 6 | Test `eleve` sans assertion par testid individuel | 🟡 | 1 | ❌ Laissé — `lockCount==5` sur page à 5 items suffit |
| 8 | (opus) `PermissionSeeder` docblock « 21 permissions » (réel : 24) | — | info | ⏳ Hors périmètre 40.2 (dette doc pré-existante, noté) |

Légende sévérité : 🔴 Critique — 🟠 Important — 🟡 Mineur
Pertinence Opus : 0 = non pertinent/invalide, 3 = très pertinent

## Verdict des deux passes

- **Sonnet** : APPROVE-WITH-CHANGES, 6 findings. Bien intentionné mais **sur-cote nettement** : #1 classé 🔴 pour un simple manque de redondance de test.
- **Opus (2e avis)** : **story SAINE, aucune correction auto strictement nécessaire.** Deux findings sonnet **invalidés** (#2 lecture erronée du commentaire de test ; #5 méconnaissance de la sémantique `??`). Seul #4 mérite une décision produit (réel mais périmètre étroit, échec = 403 propre).

## Vérifications de non-régression (opus, sur code réel) — toutes ✅

- **`computer-admin` a bien les 5 perms `computer`** (`view/control/elevate/install/remote.rdp`, `SambaRole.php:110-128`) → test `5/5` correct.
- **`technicien` = exactement `computer.view` + `computer.control`** en catégorie computer (+ `wpkg.assign` hors domaine) → test `2/5` correct.
- **`documentedDomains()`** : clés ancrées `SambaPermission::…->category()` (pas de littéraux), valeurs = routes réelles ; les 5 perms `computer` ont chacune leur entrée dans `all()` (pas de fallback sur la page).
- **Aucun des 8 tests `GuideTest.php` (40.1) cassé** par la généralisation du hub ; le seul modifié (bascule `computer`→`share` pour « Bientôt disponible ») est correct.
- **Page machines** : parité avec la page utilisateurs (`#[Computed]`, `#[Title]`, `back`, garde `Route::has()`, catégorie ancrée enum).
- **Route `app.guide.machines`** : groupe `app.` (`sambaedu.auth`), sans `can:`, avant le catch-all legacy.
- **Comptage `substr_count`** sain (pas de double-comptage `feature-computer.` vs `feature-lock-computer.`).
- **Composant `feature-guide-item` non modifié** ; gating global assumé (aucun appel `canOnWorkstationGroup`).

## Corrections appliquées

### #1 — Durcissement du garde de non-régression 40.1
**Fichier** : `tests/Feature/Guide/GuideTest.php`
Dans `hub_users_domain_is_clickable_and_others_are_coming_soon` (super-admin), ajout des assertions que la carte `domain-computer` porte désormais un `href` vers `route('app.guide.machines')`. Garantit que `documentedDomains()` conserve la clé `computer` (défense contre suppression accidentelle / conflit de merge). La couverture principale reste dans `GuideMachinesTest::hub_machines_domain_is_clickable_for_a_technicien`.

### #4 — Limite connue documentée
**Fichier** : `docs/qa/domains/guide.md` (section « Limites connues » + test manuel ciblé)
Le lien how-to « Machines » vers `/app/parc` exige `computer.view` (gate `viewAny-workstationGroup`) ; un profil custom `computer.control` sans `computer.view` obtiendrait un 403 propre au clic. Aucun rôle livré concerné. Comportement assumé (guide non scopé par parc), documenté pour QA.

## Findings écartés / laissés (non-bloquants)

- **#2, #5** : invalidés par le 2e avis opus (lectures erronées).
- **#3, #6** : durcissements cosmétiques de test, valeur négligeable (chaînes uniques en contexte, cardinalité déjà garantie).
- **#8** : `PermissionSeeder` docblock « 21 » → 24 : dette doc **pré-existante hors 40.2**, non touchée dans ce lot (éviter de mêler un fichier hors périmètre au commit).

## Tests

⚠️ **Non exécutés** (aucun runtime PHP sur l'hôte, VM sans `pdo_sqlite`). D'après l'analyse statique opus, les 6 tests `GuideMachinesTest` + les 8 `GuideTest` (dont l'assertion ajoutée) devraient tous passer (seeder itère `SambaRole::cases()` → technicien + computer-admin seedés). **À rejouer** : `php artisan test --filter=Guide`.

## Rappel déploiement VM

Nouvelle route `app.guide.machines` → `php artisan route:cache` + `chown www-admin:www-admin bootstrap/cache/*.php` côté VM (sinon catch-all legacy → 404).

## Verdict

**APPROVE.** Implémentation conforme et saine, réutilise proprement le socle 40.1 sans le modifier, généralisation data-driven du hub correcte, cardinalités de test validées par l'enum. Un durcissement de test appliqué (#1), une limite produit documentée (#4). Reste la validation des tests sur un environnement PHP. Prêt à commit sur `guides` et à réutilisation par les stories 40.3+.

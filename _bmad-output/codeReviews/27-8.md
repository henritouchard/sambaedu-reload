# Code Review — Story 27.8 : Retrait du mode strict/default de la drift policy — STRICT partout

Status: to-validate
Date: 2026-06-16
Dev model: opus (claude-opus-4-8[1m])
Review model: sonnet (1er passage)
Second review: oui (opus — 2e avis indépendant + chasse aux findings manqués)
Tests : Go `go test -count=1 ./...` + `vet` linux/windows + cross-compile **VERTS** (re-vérifiés par les 2 relecteurs). PHP `php -l` OK sur tous les fichiers touchés ; PHPUnit **différé à `/vm` post-merge** (vendor absent en worktree). Hash figé PHP↔Go concordants (`4d0c2c94…3579`), test croisé `TestHashStateGoldenMatchesFrozenHash` vert.

## Contexte particulier

Story de **retrait transverse à haut risque** : elle révise/annule une grande partie de 27.1 (introduction du mode) et 27.3 (déplacement règle→assignation), motivée par la review 27.3 (Q1/#6 : grain réel = `type × poste`, pas `item × cible` → promesse creuse). Contrairement à 27.3, **le contrat agent BOUGE** : la clé `mode` quitte l'enveloppe de chaque item (5→4 clés), le statut `drifted_allowed` disparaît → bump **croisé PHP↔Go** des hashes figés assumé sciemment (zéro prod, reste `se5.desired-state/v1`).

Changeset : 74 fichiers (+1372/−1087), 4 créés (story + 3 migrations DROP), 2 supprimés (`StateMode.php`, `StateModeCastTest.php`).

## Questions en attente (décision Henri)

> Aucune question bloquante. Les 2 questions soulevées par le 1er passage sont résolues par le 2e avis :
> - **Store `AppliedState` (ex-finding #3)** : le 2e avis Opus a établi que ce n'est **PAS du code mort** — il est lu/muté/persisté par `companion.go` (`ReadAppliedState`/`WriteAppliedState`) et `engine.go:230-233` (mutation `applied[typ]` horodatée). Seul `lastApplied` **au verdict** a été découplé (`ResolveItemStatus` simplifié) — découplage **intentionnel** (décision D-B). Rien à corriger.
> - **Finding doc #1** : corrigé dans cette story (cf. ci-dessous).

Aucune décision utilisateur requise pour avancer.

## Synthèse des problèmes

| # | Problème | Sévérité | Pertinence Opus | Statut |
|---|----------|----------|-----------------|--------|
| 1 | `contract-v1.md:96` — le *wrapper figé* liste encore `type/semantics/mode/payload/hash` (se contredit avec §3/§5 du même fichier) | 🟡 | 3 (manqué par l'AC9, vu par sonnet) | ✅ Corrigé |
| 2 | `stats-cards.blade.php:97-98` — commentaire « dérive tolérée distincte » réfère `drifted_allowed` retiré | 🟡 | 2 | ✅ Corrigé |
| 3 | `engine.go` — `AppliedState` supposé « code mort » | 🟡 | 1 (réfuté) | ✅ Clarifié (n'est PAS mort — D-B intentionnel) |
| 4 | `report.v1.json` — pas de type `drives`/`shortcuts` | 🟡 | 2 | ✅ Non-bug confirmé (aucun hash report ; 3 statuts illustrés) |
| 5 | `create_agent_report_tables.php:43` — commentaire colonne `status` mentionne encore `drifted_allowed` (**manqué par sonnet**, trouvé par Opus) | 🟡 | — | ✅ Corrigé |

Légende sévérité : 🔴 Critique — 🟠 Important — 🟡 Mineur
Pertinence Opus : 0 = non pertinent → 3 = très pertinent, — = hors notation / trouvé par Opus

**Aucun finding 🔴/🟠.** Les 5 findings sont des résidus documentaires/commentaires ; 3 corrigés, 2 requalifiés (non-bug / non-mort). Zéro impact runtime, zéro test cassé.

## Détail des problèmes

### #1 — `contract-v1.md` : le wrapper figé liste encore `mode` 🟡 → ✅ Corrigé

**Fichier(s)** : `docs/agent/contract-v1.md:96`

**Constat (sonnet, Opus pertinence 3)** : §3.2 décrivait « le *wrapper* (`type/semantics/mode/payload/hash`) » alors que §3 (L66-67) et §5 (L158-175) du même fichier déclarent le retrait de `mode` (item à 4 clés). Auto-contradiction sur la **définition du wire format figé** — c'est l'objet le plus normatif du doc. L'AC9 demandait « le tableau des clés perd `mode` » : le tableau avait été nettoyé, cette mention en prose ratée.

**Correction appliquée** : `type/semantics/payload/hash` + note `(Story 27.8 : la clé mode a été retirée du wrapper — convergence strict inconditionnelle, cf. §5)`.

### #2 — `stats-cards.blade.php` : commentaire « dérive tolérée distincte » 🟡 → ✅ Corrigé

**Fichier(s)** : `resources/views/pages/parc/_partials/stats-cards.blade.php:97-98`

**Constat (sonnet, Opus pertinence 2)** : commentaire Blade « la dérive tolérée est distincte (jamais comptée comme écart) » référant une catégorie `drifted_allowed` supprimée. Code fonctionnel correct (compteurs `enrolled`/`exceptions`/`compliant`/`silent` intacts) — résidu purement cosmétique.

**Correction appliquée** : « En écart » = drift+error (Story 27.8 : statut drifted_allowed retiré — la cible fait toujours loi, convergence stricte).

### #3 — `AppliedState` « code mort » 🟡 → ✅ Clarifié (réfuté)

**Fichier(s)** : `agent/shared/engine.go:226-234`, `agent/shared/sessionstore.go`, `agent/shared/companion.go:171-180`

**Constat initial (sonnet, pertinence 1)** : `lastAppliedHash` n'étant plus passé à `ResolveItemStatus`, sonnet a supposé l'`AppliedState` écrit-mais-non-lu.

**Avis Opus (réfutation)** : `AppliedState` est **vivant** — `companion.go:171` le lit (`ReadAppliedState`), `:176` le passe à `RunPass`, `:180` le réécrit (`WriteAppliedState`) ; `engine.go:230-233` mute `applied[typ]` avec `AppliedAt` horodaté à chaque passe. Il est lu, muté, persisté et observable (traçabilité dernier-appliqué, décision 24.4 n° 9). Ce qui est découplé = l'usage de `lastApplied` **au verdict**, retiré volontairement (D-B). **Pas de code mort.** Aucune action requise (le docblock `décision 24.4 n° 9` documente déjà l'intention).

### #4 — `report.v1.json` sans type `drives`/`shortcuts` 🟡 → ✅ Non-bug confirmé

**Fichier(s)** : `tests/Fixtures/Agent/report.v1.json`

**Constat (sonnet, Opus pertinence 2)** : le golden report illustre 3 statuts sur wallpaper/overlay/printers ; drives/shortcuts absents.

**Avis Opus** : non-bug confirmé, et **aucun hash figé du report n'existe** (vérifié : pas de `frozenReportHash`/`HashReport` côté PHP ni Go — le report est du transport non hashé, `contract.go:147-148`). Le test `report_…_three_statuses` exige seulement que les 3 cas de `AgentResourceStatus::cases()` soient illustrés — satisfait. Le choix des types est libre.

### #5 — Migration historique : commentaire colonne `status` 🟡 → ✅ Corrigé (manqué par sonnet)

**Fichier(s)** : `database/migrations/2026_06_11_140000_create_agent_report_tables.php:43`

**Constat (2e avis Opus, manqué par sonnet)** : le `->comment()` de la colonne `status` disait encore `compliant|drift|drifted_allowed|error`. La story (piège n° 3) demandait explicitement de vérifier ce point. **Aucun risque runtime** : c'est un commentaire SQL, pas une contrainte CHECK ; et `ReportRequest:59` valide `items.*.status` via `Rule::enum(AgentResourceStatus::class)` AVANT ingestion → un `drifted_allowed` entrant est rejeté en 422, jamais `::from()`-é (pas de `ValueError`). Seul vecteur théorique = hydratation d'une ligne `drifted_allowed` préexistante (zéro prod, aucune ligne).

**Correction appliquée** : commentaire → `compliant|drift|error (AgentResourceStatus) — Story 27.8 a retiré drifted_allowed` (accurate + conserve l'historique de révision plutôt que d'effacer une migration jouée).

## Points vérifiés CONFORMES (anti-régression — les 2 relecteurs)

**Contrat / Golden (priorité 1)**
- `state.v1.json` : 0 occurrence `"mode"`, 5 items à exactement 4 clés ; schéma reste `se5.desired-state/v1` (D-A).
- `FROZEN_STATE_HASH` (PHP) == `frozenStateHash` (Go) = `4d0c2c9406c448c8febb05807f33bb8c53af17aec0c9051ca7a4d4fddbf93579`. Tests croisés `TestHashStateGoldenMatchesFrozenHash` + `TestHashItemGoldenItemsMatchTheirHashFields` (5 items) **verts** (relancés indépendamment par les 2 relecteurs).
- `report.v1.json` : item `drifted_allowed` retiré, 3 statuts illustrés.
- `StateHasher.php` modifié = **uniquement un commentaire docblock** (pas de changement de canonicalisation) — vérifié axe G.

**Agent Go**
- `ResolveItemStatus(isCompliant bool)` : 2 cas (`compliant→ShouldPersist` / `drift→ShouldApply+ShouldPersist`, les deux à true), branche `humanDrift`/`drifted_allowed` absente.
- `StateItem.Mode` + parsing `"mode"` retirés ; `RunPass` sans résolution mode.
- **CONSERVÉS** (D-B) : `AppliedState`/persistance horodatée, `AggregateHash`, isolation par type, ordre serveur.
- `contract.go::ResourceStatuses` = `["compliant","drift","error"]`.
- `go test`/`vet` linux+windows/cross-compile **verts** (re-vérifiés from scratch).

**Serveur / Providers**
- `grep StateMode app/ tests/` VIDE (enum supprimé, 0 import orphelin) ; `StateCandidate` à 4 paramètres ; `StateProvider::mode()` + `StateCompiler::aggregateMode()` retirés ; item compilé à 4 clés.
- 5 providers sans `mode()`/lecture colonne/passage `mode:` ; ciblage/payload/sémantique INCHANGÉS ; NFR7 (`ldap|apcu|samba-tool`) = docblocks seulement.
- Conformité robuste : `ConformityService::summary` `match` avec bras `default` (pas d'`UnhandledMatchError`), `WorkstationGroupRepository` `whereIn` sur littéraux (pas d'enum exhaustif cassé), aucun total/pourcentage faussé (axe B).
- `AgentResourceStatus` 3 cas ; `ReportRequest` valide en amont (pas de `ValueError` à l'ingestion).

**Migrations**
- 3 DROP idempotentes (`Schema::hasColumn` des 2 côtés), `down()` recrée `string('mode',16)->nullable()` **identique** aux `add_mode_*` d'origine (sans DEFAULT), datées `110200/01/02` (> `110100` du 27.3) → `down()` global cohérent. `shortcuts.mode` non touchée.

**UI / Tests**
- 3 toggles retirés sans `wire:model="mode"` orphelin ni méthode `setMode` morte ; geste métier (assignation/wallpaper/overlay) intact.
- Tests réécrits **significatifs** (pas d'`assertTrue(true)`) : `TestRunPassStrictLifecycle` prouve « dérive → réappliquée » ; `ShortcutModeTogglePageTest` prouve l'absence de propriété/colonne mode + lien créé ; `ContractV1Test` 4 clés/3 statuts. `StateModeCastTest` supprimé.

## Verdict global

**APPROUVÉ** — mergeable. Aucun bloquant, aucun finding 🔴/🟠. Le retrait transverse est complet, cohérent et testé : la machine d'états strict est prouvée (suppression humaine → réappliquée), le hash figé concorde PHP↔Go (tests croisés verts), la chaîne de conformité serveur est robuste aux valeurs inconnues (pas de `ValueError`/`UnhandledMatchError`), les invariants D-B (store applied-state, AggregateHash, isolation) sont préservés. Les 5 findings étaient des résidus documentaires — 3 corrigés, 2 requalifiés. **Reste l'action `/vm` post-merge** (migrate 3 DROP, trash des fantômes `StateMode.php`/`StateModeCastTest.php`, PHPUnit `--filter Agent` pour valider le hash côté PHP) + la validation lab Windows par Henri (suppression humaine → réappliquée).

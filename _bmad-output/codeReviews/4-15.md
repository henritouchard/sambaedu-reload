# Code Review — Story 4.15 : Écriture SQL→AD `PP_<X>` pilotée par `is_head_teacher` + UI « Professeur principal »

Status: to-validate
Date: 2026-06-25
Dev model: opus (claude-opus-4-8[1m])
Review model: sonnet (claude-sonnet-4-6)
Second review: oui (opus — arbitrage + recalibrage des sévérités + findings manqués)

## Questions en attente

> Décisions nécessaires avant clôture. Le code est fonctionnellement correct sur le chemin nominal ; ces points relèvent du profil de charge AD et de la robustesse en mode dégradé. Aucun ne bloque le test groupé 4.13/4.14/4.15 demandé.

1. **(#3 + M1) Coût LDAP du toggle PP** — un simple toggle « Professeur principal » via la section UI appelle `updateGroup` avec `user_ids` complet → re-synchronise les 3 cibles AD (`Equipe_`/`Classe_`/`PP_`) + un `syncFromAd()` **GLOBAL** (sans `onlyGroupNames`, qui rebalaye tout l'annuaire de l'établissement et peut supprimer des groupes SQL transitoirement désynchronisés) + un write `updateGroupDescription` AD inutile (oldName==newName). Sur un parc fédéré (mémoire : 75 étab), c'est un coût massif pour un geste fréquent. Le `syncFromAd()` global préexiste dans `updateGroup` (hors-scope 4.15), mais 4.15 en fait un déclencheur d'UI fréquent. **Veux-tu scoper le read-back au seul groupe courant (`onlyGroupNames`) ? Cela touche le contrat du read-back 4.14.**

2. **(#4 + M2) Flag posé uniquement par read-back fail-soft + toast mensonger** — l'option (b) retenue (autorisée par la story, D3) n'écrit JAMAIS `is_head_teacher` en direct : il est posé exclusivement par le read-back `syncFromAd`, dont la boucle est fail-soft par groupe (`catch (\Throwable) { $stats['errors']++ }`). Si la lecture du CN `PP_<base>` échoue (LDAP instable) APRÈS l'écriture AD, le pivot n'est pas posé → `is_head_teacher` reste à sa valeur antérieure, **pendant que `save()` affiche un toast de succès inconditionnel** (`updateGroup` ne renvoie aucun indicateur d'échec partiel). **Acceptes-tu ce fail-soft (cohérent avec AD-first transitoire) ou veux-tu durcir : écriture pivot directe en complément (ceinture + bretelles) et/ou toast conditionné à une relecture ?**

## Synthèse des problèmes

| # | Problème | Sévérité | Pertinence Opus | Statut |
|---|----------|----------|-----------------|--------|
| M6 | Edit-form (retrait membre / save liste) **efface les PP** : `head_teacher_ids` absent → `[]` → `PP_<base>` vidé + pivot effacé par read-back | 🔴 | 3 (manqué par sonnet) | ✅ Corrigé |
| 3 | Toggle PP → re-sync AD 3 cibles + `syncFromAd` GLOBAL + write description inutile (coût LDAP) | 🟠 | 3 | ⏳ Décision (Q1) |
| 4 | `is_head_teacher` posé uniquement par read-back fail-soft → flag false silencieux | 🟠 | 3 | ⏳ Décision (Q2) |
| M2 | `save()` toast de succès inconditionnel (pas de signal d'échec partiel) | 🟠 | 2 (manqué) | ⏳ Décision (Q2) |
| 5 | Read-back résout les membres par `cn` traité comme `login` (convention SE4) → fragile en fédération/AD tiers | 🟡 | 2 | ✅ Documenté (runbook §9) |
| 6 | Pas de test de RENDU readonly pour viewer `user.read` seul (AC12 testé serveur uniquement) | 🟡 | 2 | ✅ Corrigé (test ajouté) |
| 1 | `headTeacherIds` propriété Livewire publique sans `#[Locked]` | 🟠→🟡 | 1 | ✅ Écarté (double défense suffit) |
| 2 | `profMembers()` / `groupModel()` publiques = actions wire:call sans Gate | 🟠→🟡 | 1 | ⏳ Optionnel (Q3 — risque save()) |
| 7 | Branche `head_teacher_ids` sans `user_ids` = non testée (robustesse API) | 🟡 | 1 | ✅ Couvert (test M6) |

Légende sévérité : 🔴 Critique — 🟠 Important — 🟡 Mineur
Pertinence Opus : 0 = non pertinent, 3 = très pertinent. Les flèches (🟠→🟡) = recalibrage par le second avis opus.

## Détail des problèmes

### #M6 — Edit-form efface silencieusement les professeurs principaux

**Sévérité** : 🔴 Critique
**Fichier(s)** : `app/Services/UserGroupService.php:138-175` ; déclencheurs `resources/views/pages/users/groups/[id]/index.blade.php:88,143`

**Constat (finding manqué par sonnet, confirmé par opus puis vérifié dans le code)** :
`syncRoleAwareAdGroupMembers` synchronise TOUJOURS la 3ᵉ cible `PP_<base>` (l.919). `removeMember()` et `save()` de l'edit-form appellent `updateGroup` avec `user_ids` mais **sans** la clé `head_teacher_ids`. L'ancien code posait alors `$headTeacherUserIds = []` → `PP_<base>` vidé en AD → le read-back `syncFromAd` relit un `PP_` vide → efface le pivot `is_head_teacher`. **Résultat : toute édition de membres d'une classe (même sans rapport avec les PP) supprime tous ses professeurs principaux, sans erreur ni avertissement.** Feature auto-destructrice en exploitation normale.

**Avis Opus** : 3/3. Vérifié de bout en bout (l.919 PP toujours écrit ; appelants edit-form sans `head_teacher_ids` ; chemin read-back l.150-152). Régression silencieuse réelle.

**Correctif appliqué** : distinction **clé absente** vs **`[]` explicite**. Quand `head_teacher_ids` est absent du payload, `$headTeacherUserIds` est dérivé du pivot existant (`$group->users()->wherePivot('is_head_teacher', true)->pluck('users.id')`) → les PP existants sont préservés. Un `[]` explicite (la section UI qui retire tous les PP) reste un effacement volontaire. L'intersection avec `$selectedUserIds` (l.914-918) garantit qu'un PP retiré des membres est bien retiré de `PP_`.

**Action** : ✅ Corrigé automatiquement + test de régression `it_preserves_head_teachers_when_updateGroup_omits_head_teacher_ids`.

### #3 — Toggle PP → re-sync AD massive + `syncFromAd` global

**Sévérité** : 🟠 Important
**Fichier(s)** : `head-teacher-section.blade.php:155-161` ; `UserGroupService.php:121-163`

**Constat initial (review)** : `save()` passe `user_ids` complet → branche `$hasUserIds` → re-diff des 3 cibles AD + `syncFromAd()` global.
**Avis Opus** : 3/3, sous-estimé par sonnet. Ajoute : write `updateGroupDescription` AD inutile quand oldName==newName (M1), et `syncFromAd()` global peut supprimer des groupes SQL (whereNotIn). Coût LDAP réel sur parc fédéré.
**Solutions possibles** : (a) méthode service dédiée PP-only scopant le read-back au groupe courant (`onlyGroupNames`) ; (b) accepter (geste PP peu fréquent en pratique). (a) touche le contrat read-back 4.14 (hors-scope déclaré).
**Action** : ⏳ Décision utilisateur (Q1).

### #4 + M2 — Flag via read-back fail-soft + toast inconditionnel

**Sévérité** : 🟠 Important
**Fichier(s)** : `UserGroupService.php:150-163,477-511` ; `head-teacher-section.blade.php:163`

**Constat** : aucune écriture directe de `is_head_teacher` ; le pivot dépend du read-back `syncFromAd`, fail-soft par groupe. Échec de lecture `PP_<base>` après écriture AD → flag non posé, toast succès affiché malgré tout (M2 : `updateGroup` ne signale aucun échec partiel).
**Avis Opus** : #4 = 3/3, M2 = 2/3. Nuance : l'option (b) est explicitement autorisée par la story (D3) ; risque cantonné au chemin dégradé (AD instable) ; converge en nominal (8 tests verts).
**Solutions possibles** : (a) écriture pivot directe en complément du read-back ; (b) toast conditionné à une relecture du flag ; (c) accepter (AD-first transitoire).
**Action** : ⏳ Décision utilisateur (Q2).

### #5 — Résolution par `cn` traité comme `login` (convention SE4)

**Sévérité** : 🟡 Mineur
**Fichier(s)** : `UserGroupService.php:809-813` (`resolveMemberUserIdsFromAdGroup`)
**Constat** : le read-back résout les membres `PP_` par `cn`, recherché comme `login`. Tient en SE4/SE5 (`cn = login`, mémoire `project_user_login_single_identity`) mais fragile en fédération/AD tiers. Pas une régression 4.15 (read-back = 4.14).
**Avis Opus** : 2/3. À documenter, pas à corriger ici.
**Action** : ✅ Documenté comme limite connue (runbook QA §9).

### #6 — Pas de test de rendu readonly (viewer `user.read` seul)

**Sévérité** : 🟡 Mineur
**Fichier(s)** : `tests/Feature/Livewire/Users/HeadTeacherSectionTest.php`
**Constat** : AC12 (section readonly sans `user.modify`) testé via `save()` serveur uniquement ; le chemin de rendu (`@can('update-group')` → badge vs toggle) n'avait aucun test.
**Avis Opus** : 2/3. Lacune de couverture (pas un trou de sécurité, la garde serveur est testée). Auto-corrigeable.
**Action** : ✅ Test de rendu readonly ajouté (`it_renders_readonly_for_viewer_without_modify_permission`).

### #1 — `headTeacherIds` public sans `#[Locked]`

**Sévérité** : 🟠 → 🟡
**Fichier(s)** : `head-teacher-section.blade.php:48`
**Constat** : propriété hydratable côté client.
**Avis Opus** : 1/3. La double défense neutralise le vecteur : `save()` refiltre par `profMembers()` puis le service ré-intersecte avec `$selectedUserIds` → un id forgé ne peut devenir PP que s'il est déjà un prof membre (= ce que l'UI permet). `#[Locked]` inapplicable (la prop DOIT muter via `toggleHeadTeacher`). `groupId` est `#[Locked]` → pas d'IDOR.
**Action** : ✅ Écarté (non-bloquant ; aucun vecteur résiduel).

### #2 — `profMembers()` / `groupModel()` publiques sans Gate

**Sévérité** : 🟠 → 🟡
**Fichier(s)** : `head-teacher-section.blade.php:102,190`
**Constat** : méthodes publiques = actions `wire:call` ; `profMembers()` renvoie les membres profs du groupe sans `Gate` serveur.
**Avis Opus** : 1/3. `groupModel()` renvoie un Eloquent (non sérialisable proprement, identique au patron 5.2 validé). `profMembers()` = membres profs de CE groupe (info déjà visible avec `view-group`), pas « tous les profs » → pas de fuite de privilège. Rendu déjà gardé par `@can('view-group')`.
**Note technique bloquant l'auto-fix** : `update-group` = permission Spatie `user.modify`, `view-group` = `user.read` — **distinctes**. Ajouter `Gate::authorize('view-group')` dans `profMembers()` casserait `save()` (qui appelle `profMembers()` l.149) pour un modificateur sans `user.read`.
**Solutions possibles** : (a) laisser tel quel (aligné patron 5.2) ; (b) ajouter le gate APRÈS s'être assuré que `user.modify` implique `user.read` côté rôles Spatie.
**Action** : ⏳ Optionnel — décision Henri (Q3). Non auto-corrigé (risque de régression `save()`).

### #7 — Branche `head_teacher_ids` sans `user_ids` non testée

**Sévérité** : 🟡 Mineur
**Fichier(s)** : `UserGroupService.php:142-160`
**Constat** : branche défensive d'API non empruntée par l'UI actuelle, non testée.
**Avis Opus** : 1/3. Robustesse à coût ~0, comportement correct (dérive les membres du pivot). Seul reproche = absence de test.
**Action** : ✅ Couvert indirectement par le test de régression M6 (qui exerce `updateGroup` avec/sans `head_teacher_ids`).

## Questions ouvertes (rappel)

- **Q1 (#3 + M1)** : scoper le `syncFromAd` du toggle PP au groupe courant ? (touche le contrat 4.14)
- **Q2 (#4 + M2)** : durcir la persistance du flag (écriture pivot directe + toast non mensonger) ou accepter le fail-soft AD-first ?
- **Q3 (#2)** : ajouter le `Gate::authorize('view-group')` sur `profMembers()` après vérification que `user.modify` implique `user.read` dans les rôles Spatie ?

Aucune de ces décisions ne bloque le test e2e groupé 4.13/4.14/4.15 demandé.

# Code Review — Story 38.5: Débranchement des crons et fonctions serveur résiduelles + embed legacy

Status: in-review
Date: 2026-07-10
Dev model: opus
Review model: sonnet (claude-sonnet-5)
Éval Opus des findings : oui (orchestrateur ultradev)

## Questions en attente

Aucune.

## Synthèse des problèmes

| # | Problème | Sévérité | Pertinence | Statut |
|---|----------|----------|------------|--------|
| 1 | Greenfield : avec `set -e`, un échec d'une étape antérieure d'update.sh laissait les crons legacy actifs en silence (retrait appelé trop tard dans main(), install.sh ne fait qu'un log_warning) | 🟠 | 3 | ✅ Corrigé |
| 2 | Couverture T5.1 partielle : install.sh jamais inspecté par le test d'architecture | 🟡 | 2 | ✅ Corrigé |
| 3 | Risque d'échec partiel absent du runbook QA | 🟡 | 2 | ✅ Corrigé |
| 4 | Discoverabilité : install.sh ne mentionne le retrait que par commentaire | 🟡 | 1 | 🚫 Ignoré (commentaire jugé honnête et suffisant ; couvert par #1 et #2) |

## Détail des corrections

### #1 — Retrait joué en tête de main() (pertinence 3)
`ensure_system_cron` + `ensure_legacy_crons_retired` déplacées EN TÊTE de `main()` (juste après check_root/check_artisan, avant `update_composer` — première étape susceptible d'échouer). Fonctions pures (fichiers /etc/cron.d), aucun prérequis composer/laravel. Ordre système-cron → retrait préservé (zéro fenêtre sans ticket Kerberos). Nouveau test `cron_functions_run_before_any_fallible_step` (retrait avant update_composer).

### #2 — Couverture install.sh
Nouveau test `install_script_provisions_system_cron_and_never_retires_directly` : `install_system_cron` déclarée + appelée dans install.sh, anti-glob `sambaedu-*`, aucun retrait direct de `sambaedu-web-common`/`-shares` (le retrait passe exclusivement par le replay update.sh).

### #3 — Runbook
Point 5 ajouté au scénario 38.5-1 : conduite à tenir en cas d'échec partiel d'update.sh pendant une install (rejouer avant de considérer l'install terminée).

## Points salués par la review (vérifiés indépendamment)

Liste de retrait explicite sans glob, cibles SE5/boot-server jamais mentionnées dans la fonction ; mv réversible horodaté (jamais rm -rf) ; sambaedu-system.cron iso-legacy exact (renew_ticket ×2 dont @reboot www-admin, smbstatus root, fin de fichier \n) ; embed débranché sans casser la vue `legacy-embed.blade.php` (2 consommateurs catchall intacts) ni la création de groupe native ; backlog.data.js (travail utilisateur) intact ; grep de clôture zéro référence LegacyEmbed ; échecs pré-existants recomptés (Story1614 ×3, FederatedRoute ×1) identiques et étrangers au diff.

## Vérification post-corrections

`bash -n` OK (update.sh + install.sh) ; `LegacyCronRetirementTest` + `LegacyEmbedRouteRemovedTest` + `GpoLegacyIsolationTest` → **11 passed (53 assertions)**.

## Verdict

Approuvée après corrections. Restent les ops VM manuelles (consignées story + runbook 38.5-1..4) : fantômes LegacyEmbed* à retirer à la main côté VM (inotify ne sync pas les deletes) + dump-autoload, replay update.sh, vérifs H+1 (klist, /tmp/smbstatus), et l'AC1 finale sur logs Apache. Entrées backlog à reporter par Henri (stats natives, ENT abandonné) — consignées dans le Dev Agent Record.

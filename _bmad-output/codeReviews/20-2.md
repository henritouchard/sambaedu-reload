# Code Review — Story 20.2 : Identité externe persistante — cycle de vie & base légale de rétention RGPD

Status: to-validate
Date: 2026-06-02
Dev model: claude-opus-4-8
Review model: claude-sonnet-4-6
Second review: oui (claude-opus-4-8)

## Questions en attente

> Questions nécessitant une réponse d'Henri avant de pouvoir clôturer la story (les défauts RGPD ont déjà été validés pour le dev : anonymisation jamais hard-delete, TTL 365j, anti-résurrection 403, toggle OFF par défaut).

1. **(P-4 / RGPD)** Le hash `sha256(sub)` est sans sel. Avec un `sub` à faible entropie côté IdP, il serait ré-identifiable par force brute → la valeur `anon:<sha256>` resterait *pseudonyme* (pas anonyme) au sens RGPD. **Tension** : D-5 veut un hash **re-corrélable** pour besoin forensique 20.4 (re-hasher un sub connu). Un sel via `app.key` casserait cette propriété. Faut-il (a) garder tel quel en garantissant que controlHub émet des `sub` UUID v4 (≥122 bits), ou (b) passer à un HMAC avec clé de rétention dédiée persistée ? **Arbitrage DPO.**
2. **(P-7 / Q-4 story)** La sélection de purge se fait sur le **seul critère temporel** (`last_login_at < now - pii_ttl_days`), sans exclure les identités liées à un `User` encore actif. La Q-4 de la story tranchait déjà « critère temporel seul ; hold légal = action admin future ». Confirmes-tu ce choix (cohérence spec/impl) ? *(Atténuation : une session vivante implique un `last_login_at` récent → naturellement exclue.)*
3. **(M-2 / périmètre)** Le guard `SambaEduAuthGuard::handleFederatedSession` (code **20.1**) logge `external_sub` **en clair** (`SambaEduAuthGuard.php:138`). La doctrine durcie par 20.2 (AC16/D-5) traite le `sub` comme corrélable et le hashe partout ailleurs. Aligner ce log 20.1 sur le hash est une modif **hors-scope strict 20.2** — à reporter en follow-up ou à inclure ?
4. **(Q-1 story)** Confirmer `pii_ttl_days = 365` et la formulation finale de `legal_basis` / `retention_note` dans `config/federated_auth.php` avant de passer `anonymize_enabled=true` en prod.

## Synthèse des problèmes

| # | Problème | Sévérité | Pertinence Opus | Statut |
|---|----------|----------|-----------------|--------|
| 1 | `--dry-run` bloqué par le garde `anonymize_enabled=false` (n'énumère jamais les candidats) | 🟠 | 2 | ✅ Corrigé |
| 2 | `anonymize()` sans transaction DB → état partiel irréparable (Users actifs + identité anonymisée) | 🟠 | 2 (+M-4) | ✅ Corrigé |
| 3 | Garde d'idempotence `deactivate()` (`!is_active && reason!==null`) | 🟡 | 1 | ❌ Non corrigé (faux positif — complément de motif légitime, jamais d'écrasement) |
| 4 | Hash `sha256(sub)` sans sel → ré-identification d'un sub faible entropie (RGPD) | 🟡 | 2 | ⏳ En attente (décision DPO — tension avec D-5) |
| 5 | Test e2e AC11 ne vérifie pas la survie de la ligne anonymisée | 🟡 | 1 | ❌ Non corrigé (déjà couvert par 2 tests unit) |
| 6 | Commande purge sans `--performed-by` + regex anti log-poisoning | 🟡 | 1 | ❌ Non corrigé (pas d'audit DB ni input libre → sans objet ; → 20.3 si UI) |
| 7 | Sélection purge n'exclut pas les identités liées à un `User` actif | 🟡 | 2 | ⏳ En attente (Q-4 tranche déjà « temporel seul » — confirmer) |
| 8 | `deactivated_reason`/`deleted_reason` non bornés → `QueryException` MySQL si > 255 | 🟡 | 2 | ✅ Corrigé |
| 9 | Log `sub_hash` après anonymisation = `sha256('anon:'+hash)` | 🟡 | 1 | ❌ Non corrigé (chemin pathologique inatteignable) |
| 10 | Test AC5 incomplet (is_active=false non réactivé par sync) | 🟡 | 1 | ❌ Non corrigé (garanti structurellement par le 403 avant syncProfile) |
| M-2 | Guard 20.1 logge `external_sub` en clair (incohérent doctrine AC16) | 🟡 | — | ⏳ En attente (hors-scope strict 20.2) |

Légende sévérité : 🔴 Critique — 🟠 Important — 🟡 Mineur
Pertinence Opus : 0 = non pertinent, 1 = peu, 2 = pertinent, 3 = très pertinent, — = apport Opus / pas noté

## Détail des problèmes

### #1 — `--dry-run` bloqué par le garde `anonymize_enabled=false`

**Sévérité** : 🟠 — **Pertinence Opus** : 2
**Fichier(s)** : `app/Console/Commands/FederatedPurgeIdentitiesCommand.php`

**Constat initial (review Sonnet)** : Le check `if (!$enabled) return SUCCESS;` survient avant le `if ($dryRun)`. Avec le toggle OFF par défaut (D-8), `federated:purge-identities --dry-run` ne liste jamais les candidats — inutilisable pour l'audit préventif / la validation pré-activation par le DPO.

**Avis Opus** : Réel et gênant. Un `--dry-run` est par définition sans effet de bord et devrait pouvoir énumérer même toggle OFF. Aucun risque juridique (ne modifie rien).

**Action** : ✅ Corrigé — le `--dry-run` énumère désormais les candidats même quand `anonymize_enabled=false` (le garde toggle ne bloque plus que l'exécution réelle).

### #2 — `anonymize()` sans transaction DB

**Sévérité** : 🟠 — **Pertinence Opus** : 2 (renforcé par M-4)
**Fichier(s)** : `app/Auth/Federated/ExternalIdentityLifecycleService.php`

**Constat initial (review Sonnet)** : `anonymize()` enchaîne `identity->save()` (PII vidée + sub réécrit + anonymized_at), puis `foreach users -> save()` (désactivation), puis `identity->delete()` (soft-delete) — sans transaction. Si un `save()` User échoue au milieu, l'identité est anonymisée mais des Users restent `is_active=true` → accès résiduel post-purge (viole AC12).

**Avis Opus** : Réel. **M-4 aggrave** : l'idempotence repose sur `isAnonymized()` qui retourne immédiatement au rejeu → l'état partiel devient **irréparable par rejeu de la commande** (les Users restés actifs ne seront jamais re-désactivés). C'est la vraie raison de transactionner.

**Action** : ✅ Corrigé — corps d'`anonymize()` (et de `softDeleteWithReason()` par symétrie) enveloppé dans `DB::transaction()`.

### #3 — Garde d'idempotence `deactivate()`

**Sévérité** : 🟡 — **Pertinence Opus** : 1
**Fichier(s)** : `app/Auth/Federated/ExternalIdentityLifecycleService.php`

**Constat initial (review Sonnet)** : La garde `!is_active && deactivated_reason !== null` n'attrape pas le cas `is_active=false + reason=null` → `deactivate()` écrit le motif.

**Avis Opus** : Partiellement faux. Écrire un motif sur une identité qui n'en avait pas est un **complément légitime**, pas une corruption. Aucun chemin ne ré-écrase un motif existant (cas nominal bloqué). Pas un bug.

**Action** : ❌ Non corrigé (faux positif).

### #4 — Hash `sha256(sub)` sans sel (RGPD)

**Sévérité** : 🟡 — **Pertinence Opus** : 2
**Fichier(s)** : `app/Auth/Federated/ExternalIdentityLifecycleService.php` (`hashSub`)

**Constat initial (review Sonnet)** : `hash('sha256', $sub)` sans sel ; un `sub` à faible entropie est ré-identifiable par bruteforce/rainbow → `anon:<sha256>` resterait pseudonyme, invalidant l'argument « plus une donnée personnelle ».

**Avis Opus** : Pertinent mais c'est un **choix de conception en tension directe avec D-5** (le hash doit rester re-corrélable pour le forensique 20.4 : re-hasher un sub connu). Un sel `app.key` casserait cette propriété. Un HMAC à clé dédiée persistée serait un compromis. Arbitrage DPO — **ne pas corriger automatiquement**.

**Solutions possibles** :
1. Garder `sha256` nu en exigeant contractuellement (20.5) que controlHub émette des `sub` UUID v4 (≥122 bits → bruteforce impraticable).
2. HMAC-SHA256 avec une clé de rétention dédiée persistée (re-corrélable côté SE5, non-réversible pour un tiers).

**Action** : ⏳ En attente de décision Henri (Q-1 du document).

### #5 — Test e2e AC11 ne vérifie pas la survie de la ligne anonymisée

**Sévérité** : 🟡 — **Pertinence Opus** : 1
**Fichier(s)** : `tests/Feature/Auth/Federated/FederatedLoginEndpointTest.php`

**Constat initial (review Sonnet)** : L'e2e vérifie que le sub clair a disparu mais pas que la ligne `anon:<hash>` survit ; un `forceDelete()` introduit à tort ne serait pas détecté en e2e.

**Avis Opus** : Déjà couvert par les unit `anonymize_clears_pii_rewrites_sub_and_keeps_row` et `anonymize_never_hard_deletes`. Redondance e2e souhaitable mais pas un trou.

**Action** : ❌ Non corrigé (couvert ailleurs ; amélioration optionnelle).

### #6 — Commande purge sans `--performed-by` + validation anti log-poisoning

**Sévérité** : 🟡 — **Pertinence Opus** : 1
**Fichier(s)** : `app/Console/Commands/FederatedPurgeIdentitiesCommand.php`

**Constat initial (review Sonnet)** : Écart vs patron `TrashPurgeCommand` (Dev Notes mentionnent `--performed-by` + regex).

**Avis Opus** : Faux écart de fond. La regex de `TrashPurgeCommand` protège un champ humain écrit dans `QuotaAuditLog`. Ici la commande **n'accepte aucun input libre** et **n'écrit pas d'audit DB** (audit = 20.4, hors-scope) → pas de surface log-poisoning, `--performed-by` sans objet.

**Action** : ❌ Non corrigé (sans objet ; à réintroduire en 20.3 si déclenchement UI manuel).

### #7 — Sélection purge n'exclut pas les identités liées à un `User` actif

**Sévérité** : 🟡 — **Pertinence Opus** : 2
**Fichier(s)** : `app/Console/Commands/FederatedPurgeIdentitiesCommand.php`, `app/Models/ExternalIdentity.php` (`scopeRetentionExpired`)

**Constat initial (review Sonnet)** : La story (IN-SCOPE §4) dit « ET sans `User` actif rattaché à une session vivante (best-effort) » — non implémenté.

**Avis Opus** : Réel écart vs cette phrase, mais la **Q-4 de la story tranche déjà** « purge sur le seul critère temporel ; hold légal = action admin future ». Choix assumé documenté. Atténuation : une session vivante implique un `last_login_at` récent → naturellement hors fenêtre.

**Action** : ⏳ En attente confirmation Henri (Q-2 du document).

### #8 — `reason` non borné → `QueryException` MySQL

**Sévérité** : 🟡 — **Pertinence Opus** : 2
**Fichier(s)** : `app/Auth/Federated/ExternalIdentityLifecycleService.php` (`deactivate`, `softDeleteWithReason`)

**Constat initial (review Sonnet)** : `deactivated_reason`/`deleted_reason` sont `varchar(255)` ; un `$reason` > 255 lève en prod MySQL (SQLite de test ne contraint pas → non détecté).

**Avis Opus** : Réel et classique. Fix non-ambigu : troncature `mb_substr($reason, 0, 255)` (additive, pas de changement de schéma).

**Action** : ✅ Corrigé — `$reason` tronqué à 255 dans `deactivate()` et `softDeleteWithReason()`.

### #9 — Log `sub_hash` après anonymisation

**Sévérité** : 🟡 — **Pertinence Opus** : 1
**Fichier(s)** : `app/Auth/Federated/ExternalIdentityLifecycleService.php`

**Constat initial (review Sonnet)** : Si `deactivate()` est appelée après `anonymize()`, `hashSub($identity->external_sub)` hashe `anon:<hash>`, rompant la corrélation forensique.

**Avis Opus** : `anonymize()` logge correctement `hashSub($originalSub)` capturé avant réécriture. Le cas tordu (deactivate après anonymize) n'est atteint par aucun chemin réel. Cosmétique.

**Action** : ❌ Non corrigé (chemin inatteignable).

### #10 — Test AC5 incomplet

**Sévérité** : 🟡 — **Pertinence Opus** : 1
**Fichier(s)** : `tests/Unit/Auth/Federated/ExternalIdentityLifecycleServiceTest.php`

**Constat initial (review Sonnet)** : Le test ne vérifie pas qu'une identité `is_active=false` n'est pas réactivée par la sync profil.

**Avis Opus** : Garanti structurellement — `reconcileOnLogin` throw 403 **avant** `syncProfile` pour une identité désactivée (`reconcile_refuses_deactivated_identity_with_403`). Trou de test mineur, comportement déjà couvert.

**Action** : ❌ Non corrigé (garanti par construction).

### M-2 — Guard 20.1 logge `external_sub` en clair (apport Opus)

**Sévérité** : 🟡 — **Pertinence Opus** : — (apport du second review)
**Fichier(s)** : `app/Auth/Federated/SambaEduAuthGuard.php:138`

**Constat (Opus)** : Le guard `handleFederatedSession` logge `'sub' => $identity?->external_sub` en clair. La doctrine durcie par 20.2 (AC16/D-5) traite le `sub` comme corrélable et le hashe partout ailleurs dans le service. Incohérence — mais c'est du code **20.1, hors-scope strict 20.2**.

**Action** : ⏳ En attente de décision de périmètre Henri (Q-3 du document) — follow-up recommandé.

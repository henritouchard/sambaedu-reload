# Code Review — Story 35.2: Type `registry_list` — listes à sous-clés indexées `\N`

Status: to-validate
Date: 2026-07-03
Dev model: fable (claude-fable-5)
Review model: opus (claude-opus-4-8)
Éval Opus des findings : non (review opus exploitée directement par l'orchestrateur ; finding #1 tranché par preuve détenue en session)

## Questions en attente

Aucune. La question du reviewer (#1, format Chrome Forcelist) est tranchée par la preuve : l'orchestrateur a lu lui-même `GPO_spécialesCD95/ExtensionPix/{CD4DCC14-…}/DomainSysvol/GPO/Machine/Preferences/Registry/Registry.xml` avant le dev — l'entrée Chrome y est en **id SEUL** (`pgpjajcmfbfdmcgjlbiengidaknopaok`), l'entrée Edge en `id;https://clients2.google.com/service/update2/crx`. Le seed est iso-GPO à l'octet, conformément à la règle de fidélité de l'epic ; c'est la story (Task 1.3 `id;url` pour les deux) qui était imprécise, pas le code.

## Synthèse des problèmes

| # | Problème | Sévérité | Pertinence | Statut |
|---|----------|----------|------------|--------|
| 1 | Écart golden/seed vs story : Chrome Forcelist en id seul (story prescrivait id;url) | 🟠 | — | ✅ Non-problème prouvé (source Registry.xml = id seul ; iso-GPO prime, Dev Agent Record l'assumait déjà) |
| 2 | `CapabilitySpecCollisionGuard` sans appelant runtime (invariant de test seul) | 🟡 | — | 🚫 Assumé/documenté — DETTE : la future UI d'édition de spec DOIT câbler ce service |
| 3 | Le guard ne validait pas hive/path non vides (conteneur vide = {status:error} silencieux côté agent) | 🟡 | — | ✅ Corrigé |
| 4 | Scope `name:""` pré-engage la surface 35.5 dans le bump 2.4.0 | 🟡 | — | 🚫 Voulu (décision d'orchestration D1 du fichier ultradev/35-questions.md) — signalé, byte-identité registry sous garde |

Légende sévérité : 🔴 Critique — 🟠 Important — 🟡 Mineur
Pertinence : — = review opus exploitée directement

## Détail des corrections

### #3 — Validation hive/path du conteneur

**Fichier(s)** : `app/Services/Agent/Providers/CapabilitySpecCollisionGuard.php`, `tests/Feature/Migrations/CapabilitiesSchemaAndSeedTest.php`

**Solution appliquée** : violation « hive et path sont requis » quand l'un est vide/blanc (refus AMONT, cohérent avec le rejet handler) + test `guard_refuses_a_list_container_with_empty_hive_or_path` (2 cas).

## Vérifications post-corrections

- PHP : `CapabilitiesSchemaAndSeed|ContractV1|StateHasher|CapabilityRegistry` → **85 passed (649 assertions), 0 failed**.
- Go : `go test ./...` ok ; `GOOS=windows build/vet` OK (inchangés par la correction, PHP-only).
- Hashes figés jumeaux PHP==Go inchangés (item `e0a9c51e…`, état `fe8eb6ea…`) ; StateCompiler/engine.go/report.v1.json intouchés.

## Verdict global (review opus)

Implémentation solide et conforme au cadrage (D1-D5, garde-fous intouchés, hashes jumeaux recalculés, réconciliation D3 correcte et bien testée). Aucun défaut de correction bloquant ; l'écart Chrome était une fidélité iso-GPO assumée, prouvée à la source.

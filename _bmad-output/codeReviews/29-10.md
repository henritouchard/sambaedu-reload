# Code Review — Story 29.10: Assainissement du tracking `queue_task_runs`

Status: done
Verdict: APPROVE WITH CHANGES (corrections appliquées)
Date: 2026-06-29
Dev model: sonnet (claude-sonnet-4-6)
Review model: opus (claude-opus-4-8[1m])
Second review: non (opus = relecteur primaire)

## Décisions Henri (pré-dev, rappel)

- Rétention **configurable par statut** : `done` 14 j / `failed` 30 j (défauts), clés `config('sambaedu.workers.retention.{done,failed}_days')`.
- **Pas** de flag de coupure (tracking toujours actif, coût géré par mémoïsation).
- **Supprimer** `configureRateLimits()` + nettoyer le TODO `discovery`.

## Synthèse des problèmes

| # | Problème | Sévérité | Pertinence Opus | Statut |
|---|----------|----------|-----------------|--------|
| 1 | Mémoïsation à `false` permanente + commentaires `after`/`failing` devenus trompeurs (chemin INSERT inatteignable) | 🟡 | — | ✅ Corrigé (mémoïse uniquement le `true` : steady-state coût/job=0 ET greenfield préservé → commentaires re-valides) |
| 2 | Planification `->daily()` (00:00) empile la fenêtre minuit, rompt la discipline d'échelonnement 24.1 | 🟡 | — | ✅ Corrigé (`->dailyAt('02:40')`, slot libre de la fenêtre maintenance) |
| 3 | `DELETE` non borné/non chunké sur table dormante ~3,5 mois (1er passage prod gros volume) | 🟡 | — | ⏳ Accepté (parité stricte avec `PruneAgentReportsCommand` ; volume borné — table n'accumule que depuis le merge 29.9) |
| 4 | Nouvelles clés config → piège `config:cache`+chown au déploiement VM ; `.env.example` non à jour | 🟡 | — | ✅ Corrigé (2 env vars ajoutées à `.env.example`) + rappel runbook à la finalisation |
| 5 | Ligne terminée incohérente (`done` sans `finished_at` / `failed` sans `failed_at`) échappe à jamais à la purge | 🟡 | — | ⏳ Accepté (invariant garanti par les handlers : `before` pose `started_at`, `after` `finished_at`, `failing` `failed_at` ; anomalie = ligne corrompue, hors flux normal) |

Légende sévérité : 🔴 Critique — 🟠 Important — 🟡 Mineur
Pertinence Opus : — = opus relecteur primaire (pas de second avis distinct)

**Vérifications empiriques de la review (faites, pas supposées)** : `schedule:list` confirme la commande enregistrée ; `grep configureRateLimits|throttle:discovery|throttle:se4fs-api` = 0 résidu ; filtres de purge corrects par statut, `running` jamais purgé ; Carbon partout (pas de fragilité datetime sqlite) ; aucun fichier contrat/golden/`FROZEN_STATE_HASH`/`ContractV1`/migration/`WorkerMonitoringService` touché ; R3 respecté.

## Détail & résolutions

### #1 — Mémoïsation + commentaires trompeurs (🟡) — ✅ Corrigé
`app/Providers/AppServiceProvider.php` — la mémoïsation initiale figeait aussi le `false` (table absente au 1er event → inerte à vie du worker ; commentaires `after`/`failing` sur la récupération mid-life devenus faux). **Correction** : mémoïser **uniquement le `true`** (`if (! $tableExists) { $tableExists = Schema::hasTable(...); }`). Steady-state : une seule introspection puis 0/job (AC#2 satisfait). Greenfield : re-vérification tant qu'absente → apparition mid-life captée, chemins INSERT `after`/`failing` re-atteignables, commentaires re-valides. Commentaire d'en-tête réécrit.

### #2 — Slot de planification (🟡) — ✅ Corrigé
`routes/console.php` — `->daily()` (00:00) empilait error-logs:prune + parc:prune-group. **Correction** : `->dailyAt('02:40')` (libre ; rejoint la fenêtre 24.1 : trash 02:00, federated 02:30, agent:reports:prune 02:35). Vérifié `schedule:list` → `40 2 * * *`.

### #3 — DELETE non chunké (🟡) — ⏳ Accepté
`PruneQueueTaskRunsCommand` — pas de chunk, **identique à `PruneAgentReportsCommand`** (norme du codebase). Volume borné (accumulation depuis le merge 29.9 seulement). Si le 1er passage `/vm` révèle un gros volume, chunker en suivi. Pas de divergence de pattern introduite.

### #4 — Piège config VM (🟡) — ✅ Corrigé (+ runbook)
`.env.example` — ajout de `SAMBAEDU_WORKERS_RETENTION_DONE_DAYS=14` / `_FAILED_DAYS=30`. **Runbook déploiement /vm** : après modif de `config/sambaedu.php`, `php artisan config:cache` + `chown www-admin` (cf. dette « VM config cachée ≠ synced »). Sans cache, repli sur défauts 14/30 (corrects).

### #5 — Lignes terminées incohérentes (🟡) — ⏳ Accepté
`PruneQueueTaskRunsCommand` — un `done`/`failed` sans son timestamp échapperait à la purge. **Invariant garanti** par les 3 handlers (`before`→`started_at`, `after`→`finished_at`, `failing`→`failed_at`) : une telle ligne n'existe pas via le flux normal (corruption requise). Pas de filet spéculatif (anti gold-plating). Documenté comme invariant.

## VERDICT : APPROVE WITH CHANGES → corrections appliquées

Tous les AC satisfaits et vérifiés empiriquement. 3 findings corrigés automatiquement (#1, #2, #4), 2 acceptés avec justification (#3 parité, #5 invariant). Aucun 🔴/🟠. Tests ciblés **7/7 verts** ; **suite hôte complète 4721 passed / 0 failed** (0 régression) après corrections.

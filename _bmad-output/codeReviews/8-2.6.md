# Code Review — Story 8.2.6: Orchestration complète + nettoyage

Status: done
Date: 2026-04-13
Dev model: sonnet
Review model: opus
Second review: non (opus a fait la review initiale)

## Questions en attente

> Aucune question bloquante.

## Synthèse des problèmes

| # | Problème | Sévérité | Pertinence Opus | Statut |
|---|----------|----------|-----------------|--------|
| 1 | Test `preserves_tmp2_files` ne vérifie rien (assertTrue(true)) | 🟠 | — | ✅ Corrigé |
| 2 | Tests doublons installed_at / installed_version → remplacés par test SHA hash | 🟡 | — | ✅ Corrigé |
| 3 | assertTrue(true) dans test regenerate (pattern Mockery) | 🟡 | — | ✅ Accepté |
| 4 | Commentaire trompeur placeholder `install()` dans PackageInstallerService | 🟡 | — | ✅ Corrigé |
| 5 | Pas de test E2E avec vérification SHA hash réelle | 🟡 | — | ✅ Corrigé (fusionné avec #2) |

Légende sévérité : 🔴 Critique — 🟠 Important — 🟡 Mineur
Pertinence Opus : — = opus est le reviewer initial

## Détail des problèmes

### #1 — Test preserve tmp2/ est un faux test

**Sévérité** : 🟠
**Fichier(s)** : `tests/Feature/Services/AppStore/AppStoreInstallFlowTest.php:352-373`

**Constat initial (review)** :
Le test `install_flow_on_error_preserves_tmp2_files_for_diagnosis` envoie un XML invalide, attrape l'exception, puis fait `$this->assertTrue(true)` sans vérifier que les fichiers tmp2/ persistent réellement. Le nom du test promet une vérification qui n'a pas lieu.

**Solutions possibles** :
1. Après le catch, vérifier que `glob($tmp2Dir . '/*.xml')` n'est PAS vide (le XML a été téléchargé en HTTP 200 mais parseDirectives échoue)
2. Ou vérifier que le répertoire tmp2/ existe toujours

**Action** : à corriger automatiquement

### #2 — Tests doublons installed_at / installed_version

**Sévérité** : 🟡
**Fichier(s)** : `tests/Feature/Services/AppStore/AppStoreInstallFlowTest.php:387-430`

**Constat initial (review)** :
`install_flow_sets_installed_at` (L148) et `app_store_service_installed_at_is_updated_after_success` (L387) sont quasi identiques. Idem pour `install_flow_sets_installed_version` (L170) et `app_store_service_installed_version_matches_depot_version` (L407).

**Solutions possibles** :
1. Supprimer les doublons de la section "suppression code mort" (L387-430)

**Action** : à corriger automatiquement

### #3 — assertTrue(true) dans test regenerate

**Sévérité** : 🟡
**Fichier(s)** : `tests/Feature/Services/AppStore/AppStoreInstallFlowTest.php:297`

**Constat initial (review)** :
Le `assertTrue(true)` est un pattern courant avec Mockery pour éviter le warning "risky test" de PHPUnit. L'assertion réelle est portée par `->once()` dans le mock.

**Solutions possibles** :
1. Laisser tel quel (pattern standard)

**Action** : accepté — pas de changement nécessaire

### #4 — Commentaire trompeur placeholder install()

**Sévérité** : 🟡
**Fichier(s)** : `sambaedu-reload/app/Services/AppStore/PackageInstallerService.php:36-41`

**Constat initial (review)** :
Le placeholder `install()` dit "sera implémenté en 8.2.6" mais la story a choisi de ne PAS l'implémenter (le flow reste dans AppStoreService). Le commentaire est trompeur.

**Solutions possibles** :
1. Mettre à jour le commentaire pour refléter la décision
2. Supprimer la méthode vide si elle n'a aucun appelant

**Action** : à corriger automatiquement

### #5 — Pas de test avec vérification SHA hash

**Sévérité** : 🟡
**Fichier(s)** : `tests/Feature/Services/AppStore/AppStoreInstallFlowTest.php:75`

**Constat initial (review)** :
`xml_sha` est toujours `null` dans le setUp, donc `verifyXmlHash()` skip systématiquement. Aucun test E2E ne couvre le path avec hash valide.

**Solutions possibles** :
1. Ajouter un test supplémentaire avec `xml_sha` renseigné au vrai hash SHA-512 du XML

**Action** : à corriger automatiquement

## Vérifications validées

| Vérification | Résultat |
|---|---|
| `downloadPackageXml()` supprimée | OK |
| `getAppStoragePath()` conservée (utilisée par uninstallApplication) | OK |
| `finalizeInstallation()` sans $xmlPath, local_xml_path = null | OK |
| @unlink($xmlPath) après finalizeInstallation, path succès uniquement | OK |
| Import Http supprimé | OK |
| Pas de faille path traversal | OK |
| Pas de régression flow existant | OK |

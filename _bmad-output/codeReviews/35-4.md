# Code Review — Story 35.4: Geste UI — override de capacité par groupe d'utilisateurs

Status: to-validate
Date: 2026-07-03
Dev model: opus (claude-opus-4-8)
Review model: sonnet (claude-sonnet-5)
Éval Opus des findings : oui (orchestrateur ultradev claude-fable-5 — code relu, PoC du reviewer vérifié)

## Questions en attente

Aucune bloquante. Un point d'information pour Henri (finding #4, non correctif) :
le gate `customize-userGroup` est **instance-wide** (hypothèse « 1 instance SE5 = 1 établissement », documentée dans le docblock de `GroupPolicy::customize()` avec point d'extension). Cohérent avec la topologie actuelle ; à re-valider si un jour une instance servait plusieurs établissements.

## Synthèse des problèmes

| # | Problème | Sévérité | Pertinence | Statut |
|---|----------|----------|------------|--------|
| 1 | Filtre d'assignabilité HKCU appliqué au listing seulement — un rejeu Livewire direct pose un override machine-only INERTE (PoC démontré par le reviewer) | 🟠 | 3 | ✅ Corrigé |
| 2 | AC5.4 verrou amont : `authorizeUpstream()` repris du parc mais AUCUN test ne l'exerce sur la surface UserGroup | 🟡 | 2 | ✅ Corrigé |
| 3 | Bi-projection (≥1 clé HKCU parmi plusieurs) jamais exercée par un test — un bug « toutes les clés doivent être HKCU » passerait inaperçu | 🟡 | 2 | ✅ Corrigé |
| 4 | Gate instance-wide : hypothèse architecturale documentée mais non vérifiée par le code | 🟡 | 1 | 🚫 Signalé (décision assumée, aucun correctif) |

Légende sévérité : 🔴 Critique — 🟠 Important — 🟡 Mineur
Pertinence : 0 = non pertinent, 1 = discutable, 2 = pertinent, 3 = très pertinent

## Détail des problèmes

### #1 — Assignabilité HKCU non re-validée dans les mutations (piège #6 de la story)

**Sévérité** : 🟠 | **Pertinence** : 3 (PoC du reviewer : `openAdd($hklmOnlyCapId)` → `saveOverride()` persistait la ligne ; même classe de defense-in-depth que `is_active`/`overrides_locked`, correctement re-gardés eux)
**Fichier(s)** : `resources/views/pages/users/groups/[id]/_partials/capabilities-section.blade.php` (`openAdd`, `saveOverride`)

**Solution appliquée** : garde `isAssignableByUserGroup()` ajoutée dans `openAdd()` ET `saveOverride()` (toast explicite « aucune clé de portée session »). Test `machine_only_capability_cannot_receive_an_override_via_direct_livewire_calls` (openAdd refusé + contournement editingCapabilityId → zéro écriture).

### #2 — Verrou amont sans couverture de test sur la surface UserGroup

**Sévérité** : 🟡 | **Pertinence** : 2
**Solution appliquée** : 2 tests transposés du patron parc (`save_override_is_blocked_by_an_upstream_lock`, `remove_override_is_blocked_by_an_upstream_lock`) — item `ControlHubContractItem` locked matchant la clé, zéro écriture / override conservé.

### #3 — Bi-projection à ruches mixtes non testée

**Sévérité** : 🟡 | **Pertinence** : 2
**Solution appliquée** : test `mixed_hive_capability_is_listed_as_assignable` (projection HKLM+HKCU → listée).

### #4 — Gate instance-wide (information)

**Sévérité** : 🟡 | **Pertinence** : 1 — décision d'architecture correctement tranchée et documentée par le dev (« la base SE5 est par-établissement », aucune délégation par-UserGroup n'existe, délégué par-salle refusé + testé). Aucun correctif ; signalé à Henri en synthèse d'epic.

## Vérifications post-corrections

- `GroupCapabilitiesSection|CapabilitiesTab|CapabilitiesOverrideAudit|CapabilityRegistryCompilation|CapabilitiesSchemaAndSeed` → **96 passed (411 assertions), 0 failed** (18+4 tests de la section, non-régression parc incluse).
- `git status` : zéro fichier `agent/**`, `app/Services/Agent/**` (hors tests), golden touché.

## Verdict global (review sonnet)

Transposition disciplinée et globalement solide du patron capabilities-tab (parc) à la maille UserGroup. Un défaut réel démontré (PoC) sur le piège #6 — corrigé ; deux trous de couverture — comblés.

# Code Review — Story 38.2: Tombstones natifs du canal client legacy

Status: in-review
Date: 2026-07-10
Dev model: opus
Review model: sonnet (claude-sonnet-5)
Éval Opus des findings : oui (orchestrateur ultradev — relecture du code pour chaque finding)

## Questions en attente

Aucune. Les 2 questions du reviewer avaient une réponse objective :
1. « Le test 27.14 doit-il évoluer ou la doctrine interdit-elle les URIs ? » → L'epic 38 a DÉJÀ tranché (D1 : tombstone ≠ canal maintenu, réponses terminales typées AVANT le catchall ; Q1 tranchée par Henri le 2026-07-03). Le test évolue : URIs admises uniquement comme routes `legacy.tombstone.*`.
2. « Volume de legacy_catchall_logs sur la VM ? » → MESURÉ (lecture seule VM) : **29 592 lignes** → lock ALTER TABLE négligeable, finding #4 déclassé et documenté.

## Synthèse des problèmes

| # | Problème | Sévérité | Pertinence | Statut |
|---|----------|----------|------------|--------|
| 1 | `DualModeCoexistenceTest::legacy_out_routes_are_extinguished` casse (vert sur HEAD, rouge avec le diff) — non vu ni déclaré par le dev | 🔴 | 3 | ✅ Corrigé |
| 2 | Chiffres de tests inexacts dans le Dev Agent Record + runbook (27/294 et « 33 tests » annoncés vs 21/103 réels) | 🟠 | 2 | ✅ Corrigé |
| 3 | AC3 non verrouillée : aucun test ne prouve que `network_out.php`/`out_printers.php` restent au catchall | 🟠 | 3 | ✅ Corrigé |
| 4 | Migration additive : lock ALTER TABLE non étudié sur une table de logs | 🟡 | 2 | ✅ Corrigé (mesuré + documenté) |
| 5 | Troncature `user_login` non testée (seul `machine` l'était) | 🟡 | 2 | ✅ Corrigé |
| 6 | Checklist gpo.md périmée (« kill-switch préservé », « 0 route gpo/*_out.php ») | 🟡 | 2 | ✅ Corrigé |
| 7 | Argument `$path` mort dans l'appel au catchall (recalculé en interne) | 🟢 | 1 | 🚫 Ignoré (inoffensif, noté) |

## Détail des corrections

### #1 — Test doctrinal 27.14 adapté (pertinence 3)
Vérifié : le test échouait réellement avec le diff stagé. L'invariant devient « chaque URI du canal éteint est soit absente, soit portée par une route nommée `legacy.tombstone.*`, jamais une route fonctionnelle » — commentaire renvoyant à D1/38.2 et aux tests tombstones qui verrouillent l'inertie des corps. `gpo/network_out.php` reste attendu ABSENT (exception Q4, catchall).

### #3 — Preuve AC3 ajoutée (pertinence 3)
Test `network_out_and_printers_stay_on_catchall_passthrough` : les deux chemins Linux hors périmètre traversent le catchall (proxy fake vérifié + log `source != tombstone` + zéro ligne tombstone).

### #2 / #4 / #5 / #6
Chiffres réels consignés (6 archi + 23 endpoints = 29 tests neufs après ajouts review) dans story + runbook ; volume table mesuré (29 592) documenté dans le Dev Agent Record ; test troncature `user_login` ajouté ; checklist 27.14 de gpo.md mise à jour (kill-switch supprimé, URIs tombstones).

## Vérification post-corrections

Suite complète du reviewer rejouée : **143 passed (742 assertions)** — LegacyCatchall, WpkgOutRoutes, IpxeLegacyRoutingNonRegression, IpxeNamespace, LegacyTombstone×2, LinuxOut/WingetOut×3, EnsureLocalRequestSettings, DualModeCoexistence. Échecs pré-existants hors périmètre inchangés : Story1614RoutesTest ×3, FederatedRouteTest ×1 (vérifiés étrangers au diff par le reviewer).

## Verdict

Approuvée après corrections. Salué par la review : corps strictement fixes (aucun écho de paramètre, testé par sentinelle), matrice `applications.php` exhaustive (phases/ack), ordre de routes verrouillé non-tautologiquement, retrait kill-switch complet (grep zéro occurrence), observabilité sans double-comptage. Restent les ops VM manuelles (migrate, retrait `LEGACY_CONFIG_CHANNEL_ENABLED` du .env VM + config:cache/route:cache + chown, retrait surcharge ffdiag `/etc`, curls e2e) — consignées dans la story et `legacy-shims.md`.

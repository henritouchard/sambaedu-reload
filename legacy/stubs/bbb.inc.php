<?php
/**
 * Stub bbb.inc.php — VENDORE IN-REPO (Story 38.4, D6).
 *
 * Corps legacy embarqué (sambaedu/includes/bbb.inc.php, 821 L) chargé via
 * require_once (dedup par realpath — les fonctions BBB ne sont déclarées
 * qu'une fois même si plusieurs modules font `include "bbb.inc.php"`). Les
 * require(vendor/autoload.php) legacy ont été retirés du corps (le bootstrap
 * SE5 autoload déjà). Plus AUCUNE délégation /var/www/sambaedu.
 */
require_once __DIR__ . '/_vendored/bbb.body.php';

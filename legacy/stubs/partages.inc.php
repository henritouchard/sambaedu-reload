<?php
/**
 * Stub partages.inc.php — VENDORE IN-REPO (Story 38.4, D6).
 *
 * Corps legacy embarqué chargé via require_once (dedup par realpath).
 * `roaming_profiles_stats()` reste guardée par function_exists dans le corps
 * (collision avec le stub gpo_deps déjà chargé par le bootstrap). Plus AUCUNE
 * délégation /var/www/sambaedu.
 */
require_once __DIR__ . '/_vendored/partages.body.php';

## section 5. Partitionnement du disque dur
# recettes de partitionnement pour double boot : un espace libre est nécessaire
# inclus par preseed.php dans debian.cfg

d-i partman-auto/init_automatically_partition select Assisté - utiliser le plus grand espace disponible

d-i partman-auto/choose_recipe select atomic

d-i partman/default_filesystem string ext4

d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

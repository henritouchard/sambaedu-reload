# QA Manuel — Index pré-prod

Checklist par domaine métier. À dérouler avant une mise en production importante. Chaque fichier dans `domains/` est un runbook stable qui s'enrichit au fil des stories (pas de réécriture, append only).

## Domaines couverts

- [ ] [rights-management](domains/rights-management.md) — Droits applicatifs, rôles Spatie, permissions, délégations périmétrées, scoping classe, profils CRUD, peuplement des groupes AD `Equipe_X` par rôle (profs → `Equipe_X`, élèves → `Classe_X` ; ACL prof effectives), scoping du Gate WPKG par périmètre (assignations profils/applications/options évaluées par salle via `assign-wpkg-workstationGroup`, délégation WPKG par-salle enfin opposable, fallback global admin préservé, defense-in-depth `AppProfileService`), scoping de `app.customize` par WorkstationGroup (override de capacité par parc gaté via `customize-workstationGroup` jumeau de `assignWpkg` + `groupId` figé `#[Locked]` serveur-autoritatif → fin du tampering `groupId` de la faille M4 ; exclusion négative par parc enfin opposable au détenteur du droit global ; verrou amont 29.2 et audit 29.5 intacts), habilitation du délégué `app.customize` positif-seul (retrait du plancher de droit GLOBAL du gate dual-purpose `modify-capability` → le droit est filtré PAR SURFACE en amont : `customize-workstationGroup` scopé sur capabilities-tab / `server.admin` global sur registry-tab ; le verrou amont devient le seul motif de refus du gate ; write-through positif-seul enfin livré, garde globale registry-tab et verrou amont 29.2 intacts) _(Stories 7.1, 7.2, 7.3, 4.12, 29.1, 29.6, 29.8)_
- [ ] [filesystem](domains/filesystem.md) — Quotas, snapshot, trash, home dirs, nettoyage natif des profils itinérants (snapshot nocturne /home/profiles, détection + purge orphelins vers _Trash_users), lecteurs réseau gérés (fondations backend : table `network_shares` + pivot polymorphe `network_share_assignables` User|UserGroup|WorkstationGroup access ro|rw, `NetworkShareService` provisioning FS/ACL POSIX sous `/var/sambaedu/Partages` — rx/rwx dérivées des grants user/group, WG=montage-seul, idempotent fail-soft, audit `quota_audit_logs`, export SMB `[partages]` hors git, extension `DrivesStateProvider` projetant les répertoires par maille en lecteurs `{letter,unc,label}` lettre auto-assignée pool M..Z, golden/contrat agent INCHANGÉS ; UI admin `/app/shares` policy dédiée `networkshare.*` + validation prédictive `NetworkShareValidator` (collision de lettre / lettre réservée / WG-montage-seul) ; templates de répertoire = table `directory_templates` + `DirectoryTemplateSeeder` (4 recettes : direction→tous, profs→élèves, user↔user, groupe) + `DirectoryTemplateService::materialize` matérialisant un `NetworkShare`+assignations d'un geste, casiers par-élève reportés 34.x) _(Stories 5.1c, 5.1d, 5.2, 26.3, 34.1, 34.2, 34.3)_
- [ ] [gpo](domains/gpo.md) — Group Policy Objects, profils itinérants, redirections, fondations natives `App\Gpo` _(Stories 1bis.18f, 16.1)_
- [ ] [wpkg-deploy](domains/wpkg-deploy.md) — Pipeline déploiement WPKG (logs, migrations, atomic write, chemins config), catalogue source unique bundle⇐module, ajout au catalogue NON-BLOQUANT (queue + progression Livewire), livraison FULL HTTP des payloads (alias Apache `/wpkg/files`, réécriture chirurgicale `<download>`→HTTP SE5 + `%SOFTWARE%`→`%TEMP%` pour les seules recettes `%SOFTWARE%`, exclusion des archives extraites serveur, retrait `/noDownload`), staging des outils WPKG partagés (`%Z%\wpkg\tools\` 7za/nircmd/tooltip déposés UNE FOIS sur le poste en HTTP via alias `/wpkg/tools` + `wpkg.cmd` qui matérialise `%Z%` en VRAI DOSSIER LOCAL au lieu du symlink SMB débranché — recettes inchangées) _(Stories 15.1, 27.6, 8.2.7, 27.19, 27.20)_
- [ ] [network](domains/network.md) — Réservations DHCP, baux actifs, import CSV, migration legacy via `/sync-from-ad`, sous-réseaux/VLAN (CRUD natif `dhcp_subnets` → export `dhcp-subnets.conf` → `make_dhcpd_conf.sh`, sous-réseau par défaut lecture seule, plages dynamiques multiples, mode dégradé) + scripts DHCP versionnés (`scripts/system/` + `ensure_dhcp_scripts()` greenfield-proof) _(Stories 8.1, 8.3)_
- [ ] [auth](domains/auth.md) — Plateforme HTTPS + JWT v1 poste↔serveur local (PKI locale, enrollment, refresh, révocation) _(Story 16.10)_
- [ ] [federated-login](domains/federated-login.md) — Login fédéré d'utilisateurs externes hors-AD (JWT signé RS256, IdP externe de confiance, anti-rejeu jti, mapping rôle, réconciliation guard) _(Story 20.1)_
- [ ] [ipxe](domains/ipxe.md) — Boot iPXE, résolution MAC/UUID, fondations Services Laravel _(Stories 3.1+)_
- [ ] [users](domains/users.md) — Listing /users, filtres audit (quota dépassé, mdp par défaut), synchro password_changed_at AD, pastille profil itinérant volumineux _(Stories 14.4, 26.3)_
- [ ] [ad-sync](domains/ad-sync.md) — Synchronisation AD observer-driven (Workstation, WorkstationGroup, …) + rename modrdn préservant objectGUID/netbootGUID _(Story 4.9)_
- [ ] [controlhub-contract](domains/controlhub-contract.md) — Contrat amont controlHub : persistance du modèle (5 tables, enums, relations, clés naturelles uniques NFR4), garde-fou R3 (aucun « central »), comportement standalone préservé (NFR3), ingestion idempotente (service `ControlHubContractIngestionService::ingest` seul écrivain : upsert+prune sur clés naturelles, normalisation `null→''` de target_label, validation enum+cohérence cible avec rollback total, singleton « ≤1 contrat actif », no-op = aucune écriture + aucun event `ControlHubContractChanged`), mapping refnum d'un label → WorkstationGroup (colonne string nullable `controlhub_label` par NOM sans FK dure, service `WorkstationGroupLabelService` assign/detach : label `free` du contrat actif uniquement, au plus 1 par groupe, réservé/inconnu refusés, même label idempotent ; helper `ControlHubContract::active()` ; sélecteur dans les pages parc édition/création gardé par `update`/`create-workstationGroup` ; standalone préservé NFR3, R3 sans « central »), garantie d'existence des groupes imposés (service `ImposedWorkstationGroupReconciler` : crée le `WorkstationGroup` absent via le chemin parc `WorkstationGroupService::createGroup` → observer → `WorkstationGroupAdSyncJob` AD réel `OU=Parcs`, `is_physical=false`, `managed_by_control_hub`+`locked=control_hub`+`controlhub_label` réservé ; confirme un existant par écriture CIBLÉE sans doublon ni écrasement d'un verrou `root` ; `save()` seulement si `isDirty()` = idempotence NFR4 ; lève le verrou `control_hub` des groupes non-imposés sans suppression — rupture totale du lien = Epic 32 ; no-op total sans contrat actif = NFR3 ; verrou de suppression = RÉUTILISATION de `deleteGroup()`/`isLocked()`, badge UI lecture seule « imposé par l'amont »), listener `ReconcileImposedWorkstationGroups` sur `ControlHubContractChanged` (28.2 non modifiée) + commande `controlhub:reconcile-imposed-groups`, R3 sans « central », **release des verrous à la rupture du lien** (Story 32.1, FR7+NFR5, ouvre l'Epic 32) : réception d'un signal de rupture EXPLICITE (commande `controlhub:sever-link` + endpoint `controlhub.auth`, service unique `ControlHubContractSeveranceService`) qui passe le contrat actif en `severed` de façon idempotente → verrous + bornage catalogue + refus de modif **tombent automatiquement** via `active()`→null (preuve, pas construction) ; conservation de la valeur courante effective des items imposés sans support local (matérialisation `capability_assignments` Q1=A, capacités locked instance→tous parcs / label→parcs portant le `controlhub_label`, permissif non matérialisé) ; supports locaux conservés (overrides + apps `managed_by_control_hub` posé par `materializeFromSource` Q2, flag conservé à la rupture) ; audit append-only `controlhub_link_audit_logs` de la transition `active→severed` (Q5, origine/acteur/contrat/récap) ; standalone + re-signal = no-op total (NFR3/idempotence), contrat agent figé intact, **indisponibilité amont et trace du lien** (Story 32.2, PRD §5.3 panne≠rupture + NFR5, clôt l'Epic 32) : prouvé par tests (`UpstreamUnavailabilityTest`, 13 cas, Q1=A pure preuve) que l'indisponibilité (silence) NE libère PAS les verrous — `active()` filtre `link_state` SEUL et ignore `received_at` / TTL (GARDE-FOU ANTI-RÉGRESSION) ; seule la rupture explicite 32.1 libère (panne = 0 écriture + 0 audit + 0 event) ; couverture NFR5 complète par construction (enum `{Active, Severed}` → 1 seule transition `active→severed` → 32.1 la trace, le silence n'est pas une transition d'état) ; reprise après silence NFR4-safe (`ingest()` no-op sur réception identique, refresh sur différente, aucun traitement spécial « sortie d'indisponibilité ») ; standalone byte-identique NFR3 (zéro requête `controlhub_contract_items`, zéro écriture) ; contrat agent figé INTACT (`active()` non-null pendant la panne → le dernier état compilé reste servi) _(Stories 28.1, 28.2, 30.2, 30.3, 32.1, 32.2)_
- [ ] [controlhub-contract](domains/controlhub-contract.md) — Contrat amont controlHub : persistance du modèle (5 tables, enums, relations, clés naturelles uniques NFR4), garde-fou R3 (aucun « central »), comportement standalone préservé (NFR3), ingestion idempotente (service `ControlHubContractIngestionService::ingest` seul écrivain : upsert+prune sur clés naturelles, normalisation `null→''` de target_label, validation enum+cohérence cible avec rollback total, singleton « ≤1 contrat actif », no-op = aucune écriture + aucun event `ControlHubContractChanged`), refus explicite de modification d'un item VERROUILLÉ amont (service `UpstreamLockResolver` mémoïsé court-circuit NFR3 + Gate `modify-capability` defense-in-depth service+UI : un item `registry`/`locked`/`instance` rend la capacité non éditable sur les deux surfaces — override par parc ET défaut diffusé — avec message « verrouillé par un contrat amont » et masquage des boutons ; `permissive`/`absent`/standalone restent éditables, identité de clé `strtolower(hive|path|name)` alignée à l'octet, label différé Epic 30) _(Stories 28.1, 28.2, 28.3, 29.2)_
- [ ] [controlhub-contract](domains/controlhub-contract.md) — Contrat amont controlHub : persistance du modèle (5 tables, enums, relations, clés naturelles uniques NFR4), garde-fou R3 (aucun « central »), comportement standalone préservé (NFR3), ingestion idempotente (service `ControlHubContractIngestionService::ingest` seul écrivain : upsert+prune sur clés naturelles, normalisation `null→''` de target_label, validation enum+cohérence cible avec rollback total, singleton « ≤1 contrat actif », no-op = aucune écriture + aucun event `ControlHubContractChanged`), refus explicite de modification d'un item VERROUILLÉ amont (service `UpstreamLockResolver` mémoïsé court-circuit NFR3 + Gate `modify-capability` defense-in-depth service+UI : un item `registry`/`locked`/`instance` rend la capacité non éditable sur les deux surfaces — override par parc ET défaut diffusé — avec message « verrouillé par un contrat amont » et masquage des boutons ; `permissive`/`absent`/standalone restent éditables, identité de clé `strtolower(hive|path|name)` alignée à l'octet, label différé Epic 30), relaxation PERMISSIVE au compilé (un override par `workstationGroup` d'un item `permissive` MORD réellement à l'état résolu : maille divergente `locked`→`Upstream` rang -1 inbattable / `permissive`→`UpstreamPermissive` rang 6 le moins spécifique = plancher que toute maille locale — défaut diffusé inclus — surcharge ; `locked` jamais relaxé ; maille interne jamais sérialisée → contrat agent/golden intacts) _(Stories 28.1, 28.2, 28.3, 29.2, 29.3)_
- [ ] [controlhub-contract](domains/controlhub-contract.md) — Contrat amont controlHub : persistance du modèle (5 tables, enums, relations, clés naturelles uniques NFR4), garde-fou R3 (aucun « central »), comportement standalone préservé (NFR3), ingestion idempotente (service `ControlHubContractIngestionService::ingest` seul écrivain : upsert+prune sur clés naturelles, normalisation `null→''` de target_label, validation enum+cohérence cible avec rollback total, singleton « ≤1 contrat actif », no-op = aucune écriture + aucun event `ControlHubContractChanged`), refus explicite de modification d'un item VERROUILLÉ amont (service `UpstreamLockResolver` mémoïsé court-circuit NFR3 + Gate `modify-capability` defense-in-depth service+UI : un item `registry`/`locked`/`instance` rend la capacité non éditable sur les deux surfaces — override par parc ET défaut diffusé — avec message « verrouillé par un contrat amont » et masquage des boutons ; `permissive`/`absent`/standalone restent éditables, identité de clé `strtolower(hive|path|name)` alignée à l'octet, label différé Epic 30), relaxation PERMISSIVE au compilé (un override par `workstationGroup` d'un item `permissive` MORD réellement à l'état résolu : maille divergente `locked`→`Upstream` rang -1 inbattable / `permissive`→`UpstreamPermissive` rang 6 le moins spécifique = plancher que toute maille locale — défaut diffusé inclus — surcharge ; `locked` jamais relaxé ; maille interne jamais sérialisée → contrat agent/golden intacts), lisibilité tri-état FR8 (badges imposé-verrouillé / imposé-permissif / local sur les deux surfaces de configuration des capacités — `UpstreamLockResolver` étendu avec `isCapabilityPermissive()` + `capabilityUpstreamStatus()`, bucketing `whereIn([Locked,Permissive])` ≤1 requête items, court-circuit NFR3 préservé, boutons d'écriture actifs pour permissif, libellé centré sur la relaxabilité — ANGLE MORT 29.3 respecté) _(Stories 28.1, 28.2, 28.3, 29.2, 29.3, 29.4)_
- [ ] [controlhub-contract](domains/controlhub-contract.md) — Contrat amont controlHub : persistance du modèle (5 tables, enums, relations, clés naturelles uniques NFR4), garde-fou R3 (aucun « central »), comportement standalone préservé (NFR3), ingestion idempotente (service `ControlHubContractIngestionService::ingest` seul écrivain : upsert+prune sur clés naturelles, normalisation `null→''` de target_label, validation enum+cohérence cible avec rollback total, singleton « ≤1 contrat actif », no-op = aucune écriture + aucun event `ControlHubContractChanged`), refus explicite de modification d'un item VERROUILLÉ amont (service `UpstreamLockResolver` mémoïsé court-circuit NFR3 + Gate `modify-capability` defense-in-depth service+UI : un item `registry`/`locked`/`instance` rend la capacité non éditable sur les deux surfaces — override par parc ET défaut diffusé — avec message « verrouillé par un contrat amont » et masquage des boutons ; `permissive`/`absent`/standalone restent éditables, identité de clé `strtolower(hive|path|name)` alignée à l'octet, label différé Epic 30), relaxation PERMISSIVE au compilé (un override par `workstationGroup` d'un item `permissive` MORD réellement à l'état résolu : maille divergente `locked`→`Upstream` rang -1 inbattable / `permissive`→`UpstreamPermissive` rang 6 le moins spécifique = plancher que toute maille locale — défaut diffusé inclus — surcharge ; `locked` jamais relaxé ; maille interne jamais sérialisée → contrat agent/golden intacts), lisibilité tri-état FR8 (badges imposé-verrouillé / imposé-permissif / local sur les deux surfaces de configuration des capacités — `UpstreamLockResolver` étendu avec `isCapabilityPermissive()` + `capabilityUpstreamStatus()`, bucketing `whereIn([Locked,Permissive])` ≤1 requête items, court-circuit NFR3 préservé, boutons d'écriture actifs pour permissif, libellé centré sur la relaxabilité — ANGLE MORT 29.3 respecté), drift STRICT PROUVÉ par construction (NFR2 — un item amont `locked` est déjà soumis au drift STRICT inconditionnel de 27.8 : injecté maille `Upstream` rang -1 → desired-state à 4 clés `{type,semantics,payload,hash}` sans marqueur de mode → réapplication STRICT côté agent `provision.Reconcile` ; 29.5 prouve la chaîne par un test SE5 sans réintroduire aucun marqueur/toggle de drift) + audit append-only des overrides par parc (NFR5 — table `capability_override_audit_logs` + modèle `CapabilityOverrideAuditLog::log()` patron MAISON QuotaAuditLog/DelegationHistory, câblé DANS la transaction de `saveOverride`/`removeOverride` pour l'atomicité acte↔trace : acteur id+login dénorm / item id+label / périmètre WorkstationGroup+nom / action create-update-delete dérivée côté serveur / old-new / `upstream_status` permissive-vs-local via UpstreamLockResolver court-circuit NFR3 / created_at ; append-only UPDATE→LogicException, FKs nullOnDelete) _(Stories 28.1, 28.2, 28.3, 29.2, 29.3, 29.4, 29.5)_
- [ ] [controlhub-contract](domains/controlhub-contract.md) — Contrat amont controlHub : persistance du modèle (5 tables, enums, relations, clés naturelles uniques NFR4), garde-fou R3 (aucun « central »), comportement standalone préservé (NFR3), ingestion idempotente (service `ControlHubContractIngestionService::ingest` seul écrivain : upsert+prune sur clés naturelles, normalisation `null→''` de target_label, validation enum+cohérence cible avec rollback total, singleton « ≤1 contrat actif », no-op = aucune écriture + aucun event `ControlHubContractChanged`), refus explicite de modification d'un item VERROUILLÉ amont (service `UpstreamLockResolver` mémoïsé court-circuit NFR3 + Gate `modify-capability` defense-in-depth service+UI : un item `registry`/`locked`/`instance` rend la capacité non éditable sur les deux surfaces — override par parc ET défaut diffusé — avec message « verrouillé par un contrat amont » et masquage des boutons ; `permissive`/`absent`/standalone restent éditables, identité de clé `strtolower(hive|path|name)` alignée à l'octet, label différé Epic 30), relaxation PERMISSIVE au compilé (un override par `workstationGroup` d'un item `permissive` MORD réellement à l'état résolu : maille divergente `locked`→`Upstream` rang -1 inbattable / `permissive`→`UpstreamPermissive` rang 6 le moins spécifique = plancher que toute maille locale — défaut diffusé inclus — surcharge ; `locked` jamais relaxé ; maille interne jamais sérialisée → contrat agent/golden intacts), lisibilité tri-état FR8 (badges imposé-verrouillé / imposé-permissif / local sur les deux surfaces de configuration des capacités — `UpstreamLockResolver` étendu avec `isCapabilityPermissive()` + `capabilityUpstreamStatus()`, bucketing `whereIn([Locked,Permissive])` ≤1 requête items, court-circuit NFR3 préservé, boutons d'écriture actifs pour permissif, libellé centré sur la relaxabilité — ANGLE MORT 29.3 respecté), drift STRICT PROUVÉ par construction (NFR2 — un item amont `locked` est déjà soumis au drift STRICT inconditionnel de 27.8 : injecté maille `Upstream` rang -1 → desired-state à 4 clés `{type,semantics,payload,hash}` sans marqueur de mode → réapplication STRICT côté agent `provision.Reconcile` ; 29.5 prouve la chaîne par un test SE5 sans réintroduire aucun marqueur/toggle de drift) + audit append-only des overrides par parc (NFR5 — table `capability_override_audit_logs` + modèle `CapabilityOverrideAuditLog::log()` patron MAISON QuotaAuditLog/DelegationHistory, câblé DANS la transaction de `saveOverride`/`removeOverride` pour l'atomicité acte↔trace : acteur id+login dénorm / item id+label / périmètre WorkstationGroup+nom / action create-update-delete dérivée côté serveur / old-new / `upstream_status` permissive-vs-local via UpstreamLockResolver court-circuit NFR3 / created_at ; append-only UPDATE→LogicException, FKs nullOnDelete), réception du vocabulaire de ciblage amont prouvée (FR9 — labels nom+`mode` libre/réservé casté `ControlHubLabelMode` et groupes imposés nom+`label_name` associé/null déjà reçus/persistés/idempotents/réconciliés par 28.1+28.2, verrouillés par `UpstreamLabelsImposedGroupsReceptionTest` SANS aucune nouvelle table/modèle/enum) + durcissement de l'intégrité référentielle `imposed_groups.label_name` (un groupe imposé désignant un label NON déclaré dans le même contrat est refusé à la réception par `InvalidUpstreamContractException` levée avant transaction → rollback total ; règle minimale « label déclaré », pas d'exigence `reserved`, pas de FK dure ; `label_name` nul/''/absent légitime) _(Stories 28.1, 28.2, 28.3, 29.2, 29.3, 29.4, 29.5, 30.1)_
- [ ] [controlhub-contract](domains/controlhub-contract.md) — Résolution d'un item ciblant un LABEL (FR12, dernier maillon de la propagation par label) : un item amont `target_type = label` s'applique à TOUT poste membre d'un `WorkstationGroup` portant `controlhub_label = <nom>` (mapping 30.2, par NOM sans FK). Toute l'expansion vit dans `UpstreamContractSource` (ZÉRO ligne `StateCompiler`/`StateMaille`/`specificity()`/`AgentServiceProvider`, D2 confiné) : `ensureResolved()` lève le filtre `target_type=instance` → `whereIn([instance,label])` et pré-groupe les items label par `target_label` dans `groupedByLabel[label][type|scope]` (même maille divergente `locked→Upstream` -1 / `permissive→UpstreamPermissive` 6 selon `enforcement_state`, même `toPayload`, `sourceId=item.id`) ; `candidatesFor(type,scope,$ctx)` réunit `instance ∪ (labels portés par le poste)` ; helper `labelsCarriedBy($ctx)` lit `WorkstationGroup.controlhub_label` via `TargetContext::workstationGroupIds()`, MÉMOÏSÉ par poste (anti-N+1 sur ~10 providers) ; `TargetContext` relayé par `UpstreamAwareProvider::itemsFor()` (1 ligne). NFR3 : court-circuit `groupedByLabel===[]` (aucun item label ou pas de contrat) → byte-identique + ZÉRO requête `workstation_groups`. FR12 « sans spécificité inter-parcs » = gratuit via 29.3 : deux items de même état = même rang → tiebreak intra-maille (`updated_at`/`sourceId`), JAMAIS par parc ni logique>physique. Collision 2 verrous contradictoires même clé : NON résolue (warning `agent.state.conflict` existant + tiebreak déterministe, pas d'état vide) — prévention prédictive = 30.5. Déterminisme NFR4 (`travel()` → même hash). R3 sans « central ». Test 28.3 `label_targeted_item_is_ignored` RÉÉCRIT (poste sans le label → non appliqué) + AC #1 ajouté _(Stories 28.1, 28.2, 28.3, 29.2, 29.3, 29.4, 29.5, 30.1, 30.2, 30.3, 30.4)_
- [ ] [controlhub-contract](domains/controlhub-contract.md) — Validation prédictive à l'assignation (FR13, dernier maillon Epic 30) : intercepter à l'assignation d'un label (`WorkstationGroupLabelService::assignLabel`) ET au rattachement d'un poste à un parc labellisé (`WorkstationGroupService` addMachineToGroup/setMachineGroups/setGroupMachines/bulkAddMachinesToGroup/assignMachineToPhysicalRoom via helper privé unique — JAMAIS les retraits) une collision INSOLUBLE verrou/verrou — deux items amont `locked` imposant des valeurs CONTRADICTOIRES sur la MÊME `exclusiveKey` d'un même poste — et REFUSER avant écriture (`UpstreamLockCollisionException`, message FR toast nommant propriété/sources/valeurs/périmètre). PRÉVENTION proactive (30.5) ≠ filet runtime observable `agent.state.conflict` (30.4) : les deux coexistent. Réutilisation STRICTE du socle 30.4 : accesseur lecture seule `UpstreamContractSource::lockedLabelCandidates()` (filtre `$groupedByLabel` à `StateMaille::Upstream` = locked SEUL ; permissif/absent jamais bloquant AC #3 ; court-circuit NFR3 si vide), `exclusiveKey()` DÉLÉGUÉE aux `KeyedExclusiveProvider` existants (registry ; shortcuts aggregate exclu) — ZÉRO dérivation réinventée. `UpstreamLockCollisionDetector` (pur lecture) + DTO `UpstreamLockCollision` ; filtre AC #8 « collision INTRODUITE par le label ajouté » (pas les pré-existantes). NFR3 : court-circuit `hasLockedLabelItems()` AVANT tout eager-load de population → zéro requête parc en standalone (test révélateur comptage de requêtes). Déterminisme NFR4 (`travel()` → message identique). ZÉRO ligne `StateCompiler`/`StateMaille`/décoration providers (D2 confiné, AC #5b — seul le binding du détecteur ajouté à `AgentServiceProvider`). R3 sans « central ». 16 tests `UpstreamLockCollisionTest` + Section 13 _(Stories 28.1, 28.2, 28.3, 29.2, 29.3, 29.4, 29.5, 30.1, 30.2, 30.3, 30.4, 30.5)_
- [ ] [controlhub-contract](domains/controlhub-contract.md) — Validation prédictive à l'assignation (FR13, dernier maillon Epic 30) : intercepter à l'assignation d'un label (`WorkstationGroupLabelService::assignLabel`) ET au rattachement d'un poste à un parc labellisé (`WorkstationGroupService` addMachineToGroup/setMachineGroups/setGroupMachines/bulkAddMachinesToGroup/assignMachineToPhysicalRoom via helper privé unique — JAMAIS les retraits) une collision INSOLUBLE verrou/verrou — deux items amont `locked` imposant des valeurs CONTRADICTOIRES sur la MÊME `exclusiveKey` d'un même poste — et REFUSER avant écriture (`UpstreamLockCollisionException`, message FR toast nommant propriété/sources/valeurs/périmètre). PRÉVENTION proactive (30.5) ≠ filet runtime observable `agent.state.conflict` (30.4) : les deux coexistent. Réutilisation STRICTE du socle 30.4 : accesseur lecture seule `UpstreamContractSource::lockedLabelCandidates()` (filtre `$groupedByLabel` à `StateMaille::Upstream` = locked SEUL ; permissif/absent jamais bloquant AC #3 ; court-circuit NFR3 si vide), `exclusiveKey()` DÉLÉGUÉE aux `KeyedExclusiveProvider` existants (registry ; shortcuts aggregate exclu) — ZÉRO dérivation réinventée. `UpstreamLockCollisionDetector` (pur lecture) + DTO `UpstreamLockCollision` ; filtre AC #8 « collision INTRODUITE par le label ajouté » (pas les pré-existantes). NFR3 : court-circuit `hasLockedLabelItems()` AVANT tout eager-load de population → zéro requête parc en standalone (test révélateur comptage de requêtes). Déterminisme NFR4 (`travel()` → message identique). ZÉRO ligne `StateCompiler`/`StateMaille`/décoration providers (D2 confiné, AC #5b — seul le binding du détecteur ajouté à `AgentServiceProvider`). R3 sans « central ». 16 tests `UpstreamLockCollisionTest` + Section 13, bornage de l'install refnum au catalogue applicatif amont (FR5 — le canal d'install est CONSERVÉ mais FILTRÉ au catalogue faisant autorité `controlhub_contract_catalog_apps` : nouveau `UpstreamCatalogResolver` mémoïsé (`isBounded`/`allowedAppIds`/`permits`, court-circuit NFR3 via `ControlHubContract::active()` = zéro requête catalogue sans contrat actif, D1 catalogue vide = pas de bornage) ; DEUX couches symétriques à 29.1 : consultation `Application::scopeInUpstreamCatalog` retire les apps hors catalogue de TOUTES les listes proposées (page parc, page machine, bulk catégorie, sélecteur de profil `listApplicationsForSelect`) — pass-through strict en standalone ; enforcement defense-in-depth `AppProfileService::assertApplicationsInUpstreamCatalog` refuse l'écriture pivot avec `ApplicationNotInUpstreamCatalogException` (toast « hors catalogue amont ») dans les 3 méthodes d'install `addApplicationsToWorkstationGroup`/`addApplicationsToWorkstation`/`addApplications`, no-op si `Auth::check()===false` ou non borné ; match `app_key==applications.app_id` string (D2) ; borner = filtrer l'AJOUT seulement (D4) — retrait et inventaire local LIBRES ; `severed` ⇒ `active()` null ⇒ bornage levé auto (release 32.1 gratuit) ; 15 tests `UpstreamCatalogBoundaryTest` + Section 14 ; R3 sans « central ») _(Stories 28.1, 28.2, 28.3, 29.2, 29.3, 29.4, 29.5, 30.1, 30.2, 30.3, 30.4, 30.5, 31.1)_
- [ ] [controlhub-contract](domains/controlhub-contract.md) — Déclenchement d'install en DÉSIR D'ÉTAT (FR6, dernière brique install Epic 31) : l'autorité amont ORDONNE l'install d'une app (item `controlhub_contract_items` `type='applications'`, `key=<app_id>`, enforcement non-`absent`, cible `instance|label` — DÉJÀ ingestible 28.1/28.2 sans whitelist de `type`) → l'ordre est PROJETÉ dans l'`applications` désiré servi à l'agent au check-in (portée `machine`). « Preuve, pas construction » : ZÉRO migration/modèle/enum/section payload/validation type. NET-NEW : `UpstreamContractSource::orderedApplicationAppIds($ctx)` (3ᵉ accesseur lecture seule mémoïsé, à côté de `candidatesFor`/`lockedLabelCandidates` ; indexation des items `applications` DANS la passe `ensureResolved()` existante — zéro requête de plus, `continue` car aucun adaptateur → zéro double comptage ; garde symétrique `target_label` non vide ; court-circuit NFR3 `[]` immédiat sans ordre → jamais `labelsCarriedBy` ; union `instance ∪ labels portés` dédup+tri `strcasecmp`) + `ApplicationsStateProvider` reçoit `UpstreamContractSource` en 2ᵉ dépendance et UNIONNE ces `app_id` à l'ensemble cible AVANT hydratation → payload `{app_id, name}` IDENTIQUE quelle que soit la source → dédup aggregate naturelle du `StateCompiler` = idempotence (un seul item, hash stable, pas de réinstall — WPKG `<check>` pilote). D3 pont au niveau ENSEMBLE pas via `UpstreamPayloadAdapter` (`toPayload` pur ne peut hydrater le `name` local → doublon) + garde anti double-injection `AgentServiceProvider` (NE PAS enregistrer d'adaptateur `applications`, décorateur reste no-op). D2 cible `instance ∪ labels portés` (réutilise `labelsCarriedBy` 30.4). D1 aggregate : `locked`==`permissive`=« app présente », `absent` (retrait) HORS scope — le retrait existe DÉJÀ par omission (`profiles.xml`/WPKG). Gap D4 (app ordonnée sans ligne `Application` locale) = skip+warn CONSERVÉ → comblé par 31.3. GARDE-FOUS : NFR3 standalone byte-identique (query log zéro requête `controlhub_contract_items` sans contrat ; zéro `controlhub_label` si contrat actif sans item `applications`), NFR4 (`travel()` hash stable + union triée), CONTRAT AGENT FIGÉ INTACT (`se5.desired-state/v1` golden `state.v1.json`/`ContractV1`/`StateHasher` non touchés, `FROZEN_STATE_HASH` inchangé — 31.2 ajoute une SOURCE d'`app_id`, pas un champ du wire format), R3 sans « central ». 11 tests `UpstreamInstallOrderTest` + Section 15 ; `WpkgSchemaBootstrapper` augmenté (`controlhub_contract_items` + `workstation_groups.controlhub_label`), approvisionnement auto d'une app ORDONNÉE depuis le dépôt SambaEdu (FR6, comble le gap D4 de 31.2) : le catalogue amont `controlhub_contract_catalog_apps` porte désormais la RÉFÉRENCE DE SOURCE par-app (« Option B », D1 — migration ADDITIVE `source_xml_url`+`source_xml_sha` nullables, clé naturelle `(controlhub_contract_id, app_key)` INCHANGÉE, ingestion `normalizeCatalogApps` lit ces champs optionnels `null/''→null`, rétrocompat NFR3) → `AppStoreService::materializeFromSource($appId,$source)` matérialise UNIQUEMENT la ligne `Application` (status `Available`, recette WPKG `xml_url`/`xml_sha`) via `firstOrCreate` SANS install serveur (jamais `installApplication()`) et SANS `Depot`/`DepotApplication`/`DepotSyncService` → provisionneur `OrderedApplicationProvisioner` (patron 30.3) câblé en listener `ProvisionOrderedApplications` sur `ControlHubContractChanged` (à côté de `ReconcileImposedWorkstationGroups`) + commande re-jouable `controlhub:provision-ordered-apps` : pour chaque ordre `type='applications'` non-`absent` dont l'`app_id` manque en inventaire, résout la source catalogue → matérialise ; court-circuit NFR3 sans contrat actif (no-op total, `DepotSyncService` JAMAIS appelé) ; résilience par app (try/catch + logs `agent.applications.provisioned`/`…provision_skipped`/`…provision_failed`, l'ingestion 28.2 jamais cassée) ; idempotence/non-écrasement (une `Application` locale préexistante jamais touchée — AC3). CONTRAT AGENT FIGÉ INTACT (`se5.desired-state/v1` golden/`ContractV1`/`StateHasher` non touchés — 31.3 matérialise une `Application` en DB, jamais le wire format). 28 tests net-new (`UpstreamOrderProvisioningTest` 14 + ingestion source 4 dans `ControlHubContractIngestionTest` + non-régression 674) + Section 16, R3 sans « central » _(Stories 28.1, 28.2, 28.3, 29.2, 29.3, 29.4, 29.5, 30.1, 30.2, 30.3, 30.4, 30.5, 31.1, 31.2, 31.3)_
- [ ] [parc](domains/parc.md) — Appartenance poste↔groupe unifiée dans le pivot global, salles physiques (invariant 1-salle-max app-only), runbook migration/rollback, environnement de poste par parc (shared/personal/nomade, précédence multi-parcs, service de résolution Postgres-only), configuration par défaut du parc (page à onglets de la couche Broadcast : wallpaper/lockscreen/registre/apps `is_parc_default`/outils agent + provisioning `ensure_*` idempotent fail-soft) _(Stories 4.11, 26.1, 27.17)_
- [ ] [agent](domains/agent.md) — Canal agent desired-state : ingestion des rapports POST /report, stockage D3 borné, agent squelette + compagnon de session, handlers wallpaper/overlay + route assets (démo palier 1), agent Go core/service SYSTEM/build signé, compagnon + handlers Go parité démo (binaire complet 2.1.x, zéro .ps1), conformité visible dans les pages parc (compteurs+badge+filtre, détail par type+événements, panneau groupe règles→exceptions) + forcer la synchro (PULL, bypass 304 / solde au report) — gate palier 1, distribution des releases serveur (binaires signés, manifest par ring = WorkstationGroup, canari/rollback), auto-update agent (double vérif SHA-256 + Authenticode, swap atomique anti-brique, report d'échec, version rapportée = preuve), enrôlement porte 2 des postes migrés (demande pending, approbation un-clic, mode campagne borné, anti-usurpation jamais débrayé — faisceau MAC/hostname/uuid), handler raccourcis (le bureau converge selon la nature du poste : chemin réseau/local résolu serveur = fix Bug C, union multi-mailles sans doublon, suppression level-triggered, première exposition du toggle strict/default rétroactif wallpaper/overlay), handlers lecteurs & imprimantes (l'imprimante de la salle = un item d'état comme les autres : provider `printers` défaut réglé par WG physique>logique + provider `drives` projection MVP-A des classes du user, handlers Go winspool/mpr natifs, union multi-mailles, suppression level-triggered, isolation serveur d'impression down → error, golden bumpé), rendu overlay VERROUILLÉ (Rainmeter portable posé par l'agent au bootstrap + écriture de `overlay.json` par le SERVICE SYSTEM au logon ACL `<SID>:R` infalsifiable + skin UTF-16 LE+BOM sans mojibake + `Rainmeter.ini` durci ACL ProgramData + watchdog compagnon, pas d'obfuscation), drift policy PAR ASSIGNATION (le mode strict/default suit la cible et plus la règle : un même raccourci verrouillé sur un parc, modifiable sur un autre — `mode` déplacé `shortcuts` → pivot `shortcut_assignables`, wallpaper/overlay déjà « par cible », toggle au geste d'assignation, contrat agent + golden INTACTS car mode jamais émis au payload), icône UPLOADÉE → asset statique servi par Apache (l'icône uploadée d'un raccourci s'affiche enfin réellement, plus de « feuille blanche » : `.ico` content-addressed `<sha>.ico` servi en DIRECT par un Alias Apache scopé — garde-fou sécurité : JAMAIS `storage/keys/pki/` — provider distingue nom-nu/chemin-réel + émet `{icon_asset, icon_checksum}` SANS url, agent fait un GET HTTP simple sans token + vérifie le SHA-256 avant écriture locale + pointe l'IconLocation dessus, convergence gracieuse si asset indisponible, backfill artisan name→content-addressed, golden bumpé sciemment PHP↔Go), catalogue de tools agent (le portable Rainmeter ET la skin deviennent des assets gérés serveur, toggleables depuis l'UI : upload validé du `.zip` portable — structure ZIP `Rainmeter.exe`+`Skins/`, SHA-256 calculé serveur, filename dérivé anti-traversal, mono-version — + manifest dédié `tools-manifest` hors items desired-state + skin SERVIE par la route agent authentifiée — l'embed `go:embed` retiré, plus de recompilation à la retouche — checksum du portable lu de l'état, toggle global no-op à la désactivation SANS désinstaller, golden overlay INTOUCHÉ), retrait du mode strict/default — STRICT partout (DÉMONTAGE du mécanisme `mode` strict/default introduit par 27.1 et déplacé par 27.3 : la review 27.3 a montré que le grain réel du mode était `type × poste` pas `item × cible` → promesse creuse ; comportement cible UNIQUE = la cible fait TOUJOURS loi, la dérive humaine est toujours corrigée, `drifted_allowed` disparaît ; profondeur schéma→agent : item du contrat 5 clés → 4, statut report 4 → 3, `FROZEN_STATE_HASH` PHP+Go bumpés croisés, colonnes `mode` des 3 tables droppées réversibles, enum `StateMode`/`StateCandidate::$mode`/`StateProvider::mode()`/`StateCompiler::aggregateMode()` supprimés, 3 toggles UI retirés, agent Go `ResolveItemStatus(isCompliant)` sans mode — `AppliedState`/`AggregateHash`/isolation conservés ; abroge les scénarios 4.4/6.6/13.5/Section 16, ajoute Section 19), réveil de l'agent au logon — cycle desired-state (RUNTIME/boucle agent, PAS un handler : au `WTS_SESSION_LOGON` la boucle Go part sur un cycle complet immédiat au lieu d'attendre le tick ~1 h/TTL 24 h — canal `wake` bufferisé 1 + `RequestWake()` non-bloquant coalescé, debounce min-interval 60 s côté boucle, send hors du recover overlay 27.1bis = réveil indépendant, canal nil-safe inerte hors Windows ; zéro impact serveur SE5/contrat/golden/migration, go.mod inchangé ; Section 20), handler registre par parc (CATALOGUE de réglages registre Windows prédéterminés activables par parc — l'admin non-expert ne tape jamais un chemin `HKLM\…` : table dédiée `registry_settings` + pivot `registry_setting_assignables` calqué shortcuts ; le réglage se COMPILE en item CONCRET `{hive,path,name,type,value}`, l'id de catalogue ne fuite JAMAIS au payload = couture pour brancher les clés brutes en v2 sans coût ; DEUX providers serveur HKLM/machine + HKCU/session mais UN handler Go générique idempotent — premier type MACHINE du canal : le service SYSTEM gagne son moteur de convergence machine ; exclusive PAR IDENTITÉ DE CLÉ `{hive,path,name}` au compilateur via marqueur `KeyedExclusiveProvider` ; INVERSION GLOBALE de précédence `logique > physique` D-Q3 propagée à wallpaper/printers ; fusion par type avant report car les 2 portées émettent `registry` ; drift STRICT réimpose, désactiver = cesser de gérer ; golden +1 item registry, `FROZEN_STATE_HASH` PHP+Go bumpé croisé 1599cc48…→2b49f008… ; catalogue initial 3 réglages HideFileExt/Hidden/EnableLUA ; Story 27.3), associations de fichiers par parc (type `associations` — programmes par défaut `.pdf`→Acrobat/`http`→Firefox appliqués au logon par le compagnon sous HKCU UserChoice : table dédiée `file_associations` + pivot `file_association_assignables` ; le réglage se COMPILE en item CONCRET `{identifier,progid,type}`, ni id de catalogue ni hash/SID au payload ; CŒUR DE RISQUE = le hash anti-tamper UserChoice porté FIDÈLEMENT de `SFTA.ps1::Get-Hash` en Go (~50 lignes : MD5 sur UTF-16LE + 2 passes de dérivation à constantes + XOR fold Base64) calculé 100% AGENT-side car il dépend du SID/FileTime/GUID experience du poste — VERROUILLÉ par tests vectoriels contre un portage indépendant ; exclusive PAR IDENTIFIANT au compilateur (`KeyedExclusiveProvider`, zéro modif compilateur), `logique > physique` D-Q3 ; ProgId cible NON installé → l'agent NE clobbe PAS la clé UserChoice existante = choix utilisateur PRÉSERVÉ, `error` NON fatal isolé, pas de réécriture en boucle ; suppression-avant-réécriture de la clé à ACL hérité ; seeder de REPRODUCTION legacy `FileAssociationSeeder` parse `default.xml` si présent sinon baseline figée FirefoxHTML/FirefoxURL/visionneuse ; drift STRICT réimpose, désactiver = cesser de gérer ; golden +1 item associations `FROZEN_STATE_HASH` PHP+Go bumpé croisé →77fb548a… ; EXTENSION WPKG-aware D-Henri n°7 : catalogue tagué `native` vs `wpkg`+`wpkg_package` (SERVEUR-only, jamais au payload) + validation PRÉDICTIVE UI par parc — `native`→applicable, `wpkg` déployé→applicable, `wpkg` non déployé→indisponible (warning EXACT + toast nommant le paquet AVANT déploiement, remplace le warning générique) ; paquets déployés du parc = group-level Eloquent PG-pur SANS cache APCu, jointure `<package id>`=`Application::app_id` VÉRIFIÉE ; provider INCHANGÉ (PG-pur émet toujours, NFR7), golden/hash/agent INTOUCHÉS ; Story 27.3bis), registre — valeur par défaut diffusée + override par parc (ÉVOLUTION serveur+UI de 27.3, PAS un nouveau type : `registry_settings.value` devient la VALEUR PAR DÉFAUT diffusée à TOUTE la flotte via la maille Broadcast — le provider émet désormais un candidat Broadcast par réglage actif de la ruche EN PLUS des candidats par maille — et `registry_setting_assignables.value` est un OVERRIDE de valeur par parc, repli sur le défaut si null ; l'override bat le défaut via la précédence logique>physique>broadcast EXISTANTE, exclusive par clé et StateCompiler INCHANGÉS ; PAS d'opt-out/tombstone — « retirer un override » = REVENIR AU DÉFAUT, l'agent re-converge — D3 ; posture par défaut SÛRE D6 : `EnableLUA` défaut 0→1 UAC ACTIVÉ, désactivation = override de parc délibéré ; warning au déclenchement D7 = colonne `registry_settings.warning` + `options` [{value,label}] pour le contrôle UI à choix fermé, NI l'un NI l'autre au payload ; DEUX surfaces UI : onglet « Registre » du parc REFONDU toggle→overrides seulement (ajouter/éditer/retirer + contrôle adapté au type + validation serveur + confirmation warning) + NOUVELLE page réglages serveur `/admin/settings/registry` éditant le DÉFAUT du catalogue ; CONTRAT & AGENT INCHANGÉS, golden/hash INTACTS car `state.v1.json` est hand-authored — jamais compilé d'un seed — donc la diffusion Broadcast ne le touche pas, ZÉRO fichier Go modifié ; canal legacy Registry.pol/GPO intouché meurt en 27.6 ; Story 27.3ter), config d'app déclarative (type `app_config` — policies de navigateur Firefox/Thunderbird via le SEUL mécanisme enterprise natif `policies.json` posé au chemin d'install par le moteur SYSTEM (correctif post-review #1 : `policies.json` machine-wide admin-write, scope `machine` — un compagnon user prendrait ACCESS_DENIED) : PAS de nouvelle table, on LIT la table métier `app_customizations` (4.8) via `AppCustomizationService::resolvePoliciesForMachine($wg, null, …)` PG+config-pur NFR7 — résolution PAR PARC niveaux 1-4 template/auto/défaut/WG ($user=null : le par-user de Firefox = le profil/Mécanisme B/roaming hors 27.4, PAS policies.json) ; aggregate PAR `app_kind` un item par app, scope `machine` ; payload `{app_kind, policies}` CONCRET jamais un id de scope, sans float §4.1 ; UN seul mécanisme — écriture atomique du `policies.json` (canonicalisation UNIFIÉE `shared.CanonicalJSON` cible↔fichier relu, idempotence json.Number — review #3), marqueur de périmètre `_sambaedu_managed` = fichier posé hors SambaEdu jamais écrasé/supprimé MAIS conflit rapporté `error` jamais `compliant` trompeur (review #7) ; level-triggered = policy retirée dé-appliquée, drift STRICT réimpose à l'octet près ; isolation chemin verrouillé/app absente → `error` isolé les autres types continuent, couplage install 27.5 = limite connue ; tiebreak multi-parcs logiques = plus petit id gagne, 2ᵉ parc ignoré (limite connue review #2) ; APP BUTÉE sans mécanisme enterprise = NON géré documenté zéro bricolage ; RECADRAGE 2026-06-17 Chrome/Edge + redirection de profil RETIRÉS (roaming → 26.x) ; AUCUNE nouvelle UI édition policies 4.8 inchangée ; golden +1 item app_config en portée machine `FROZEN_STATE_HASH` PHP+Go bumpé croisé 77fb548a…→6f0ff33e… ; Story 27.4), applications — l'agent déclenche WPKG (type `applications` — « un tuyau, deux outils » : l'agent unifie le TRANSPORT (déclencheur au cycle) PAS le moteur — WPKG reste le moteur déclaratif (dépendances, `<check>/<install>`, versions) NON absorbé ; handler MachineEngine SYSTEM (WPKG installe machine-wide — leçon 🔴 27.4 #1) DONNE l'URL du bundle (Apache statique) + DÉPOSE le profil par-hôte `profiles.xml`/`hosts.xml` localement (`%ProgramData%\SambaEdu\wpkg` — D9, zéro endpoint Laravel) + DÉCLENCHE `wpkg-client.vbs /NOTempo` (seul shell-out justifié, isolé `ApplicationsOps`) puis LIT `wpkg.xml` pour l'état par paquet (D5) ; provider `ApplicationsStateProvider` PG-pur NFR7 projette l'ensemble cible via `WorkstationPackagesResolver::computePackages` NON CACHÉE (jamais `resolve()`/APCu), payload `{app_id, name}` CONCRET jamais une recette d'install/id de scope, aggregate/scope machine/maille Broadcast (résolution déjà finale — D4) ; convergence level-triggered (poste hors ligne converge au prochain cycle, désiré⊆installé→compliant sans re-déclenchement, idempotent), échec d'install après run → `error`+detail jamais un faux `compliant` (leçon 🟠 27.4 #7) ; INVENTAIRE PAR APP rapporté en champ additif `inventory` (AC4 — évolution mineure §9) → table `agent_application_inventory` clé `(workstation_id,app_id)` level-triggered, fondation des licences à pool SANS UI, verdict du type reste PAR TYPE worst-status (grain 27.8 intact, D1) ; LIVRAISON NATIVE SE5 (D6) : shim legacy supprimé (`/wpkg/hosts.xml`+`/wpkg/profiles.xml`), bundle pré-substitué `SE4FS_NAME` généré (`php artisan wpkg:bundle`) servi en STATIQUE par Apache (`config('agent.wpkg_bundle_path')` PAS via Laravel — le client télécharge, zéro charge Laravel D7/D10), `wpkg-se4.js`+`wpkg.cmd` patchés (download HTTP au lieu de XCOPY SMB, profils locaux, namespaces wpkg.org intouchés), installeurs SMB inchangés (D11), GPO `se4_wpkg` PLUS publiée par SE5 (page wpkg-deployment : re-publication retirée, no-op informatif — l'agent seul déclencheur D2) ; golden +1 item applications portée machine `FROZEN_STATE_HASH` PHP+Go bumpé croisé 6f0ff33e…→283f391d… ; Story 27.5), config en CAPACITÉS — registre repensé capability-first (REWRITE de 27.3/27.3ter : l'admin gère des CAPACITÉS/options métier OS-agnostiques — « Afficher les extensions », « Bureau à distance », « MAJ Windows gérées »… — la clé de registre devient un détail de MÉCANISME caché ; table centrale `capabilities` + `capability_projections` (mechanism `registry`, `spec.keys[]`) + `capability_assignments` (override de VALEUR de capacité par maille) ; interpréteur de `spec` D5 : `value` = littéral toujours-émis (scalaire/liste MULTI_SZ via `array_is_list`) OU map valeur-capacité→donnée `{"on":0,"off":1}` dont une clé absente CESSE de gérer la clé, coercition par type zéro float ; Broadcast (défaut diffusé) + override par parc D4 « retirer = revenir au défaut » ; DEUX providers `Registry{Machine,User}CapabilityProvider` HKLM/HKCU, UN handler Go générique ; INVARIANT CENTRAL — ni id/key de capacité/projection au payload, l'item reste `{hive,path,name,type,value}` ; CONTRAT + `StateCompiler` + golden + agent Go INCHANGÉS, `ContractV1Test` vert sans modif, `FROZEN_STATE_HASH` NON bumpé, zéro fichier Go modifié ; bascule = retrait des anciennes `registry_settings`/`registry_setting_assignables`/providers/UI registre (droppées, zéro prod) ; lot iso 9 capacités windows dont migration des 3 réglages 27.3ter — EnableLUA défaut on posture sûre + warning ; bundle WindowsUpdate transcrit PARTIELLEMENT — source VM inaccessible, à compléter ; verbe `delete`/substitution `%SE4FS%` exclus ; UI onglet parc « Options/Capacités » (Gate `app.customize`) + page serveur `/admin/settings/capabilities` (Gate `server.admin`) ; Story 27.12) _(Stories 24.1 → 25.3, 27.1, 27.2, 27.1bis, 27.3, 27.3bis, 27.3ter, 27.4, 27.5, 27.7, 25.6, 27.8, 27.9, 27.12)_
- [ ] [extensions](domains/extensions.md) — Système d'extensions SE5 (socle) : registre local MULTI-SOURCES dès la fondation (tables `extension_sources` + `extensions`, clé naturelle `(source, key)`, `jsonb`/`json` branché driver, `url`/`publisher`/`icon`/`description` `NOT NULL DEFAULT ''` — jamais nullable dans une clé), enums `ExtensionType`/`ExtensionStatus`/`ExtensionSourceKind` (string + backed enum PHP, aucun `enum()` DB), manifest v1 = **contrat public** validé par un service PUR (`ExtensionManifestValidator` : version STRICTE négociée AVANT le contenu — `"1.0"`/`"v1"`/`2` rejetés sans repli tolérant, iso-décision 33.2 — champs requis, `type` ∈ enum, `id` slug, `visibility.roles` tableau non vide de rôles MÉTIER jamais des `SambaPermission`), exception dédiée `InvalidExtensionManifestException` portant le CHAMP fautif, synchro de la source EMBARQUÉE (`ExtensionCatalogService::syncBundled()` : glob `resources/extensions/<id>/manifest.json` via `config('extensions.bundled_path')` surchargeable, upsert sur clé naturelle, un manifest fautif ignoré avec `Log::warning` structuré SANS casser les autres, idempotence stricte `isDirty()` = zéro écriture au re-jeu, **`status` JAMAIS écrit** — hors `$fillable` : un rechargement de catalogue ne dé-intègre jamais une extension —, prune BORNÉ à la source bundled `available` avec conservation signalée des `integrated`), seeder idempotent `BundledExtensionSeeder` à compteurs enregistré dans `DatabaseSeeder`, pages admin LECTURE SEULE `/admin/extensions` (bibliothèque en cartes : nom, icône, type, éditeur, source, état, version ; état vide propre) et `/admin/extensions/{id}` (fiche pilotée par le manifest : version, description, scopes DEMANDÉS affichés jamais consommés FR3, dépendances, cible, public visé ; listes vides rendues proprement « Aucun scope demandé »/« Aucune dépendance » ; 404 sur id inconnu, `whereNumber` sur la route), protection 3 couches (groupe admin + `can:server.admin` par route + double garde `Gate::allows` dans `mount()`, aucune nouvelle `SambaPermission`), entrée sidebar « Extensions » du bloc Serveur, NFR15 (aucun Eloquent dans les SFC — tout passe par `ExtensionCatalogService`), et surtout **frontière NFR14 PROUVÉE** : `UpstreamSyncExtensionsBoundaryTest` exécute la cascade RÉELLE de la sync amont (ingestion + 3 listeners synchrones `ReconcileImposedWorkstationGroups`/`ProvisionOrderedApplications`/`ReconcileImposedDepot`, rupture de lien, `SyncManifestService::apply()` + `pass3Cleanup`) SANS `Event::fake()` et vérifie zéro requête SQL sur les identifiants QUOTÉS `"extensions"`/`"extension_sources"` (needles quotés : `extensions` nu matcherait `extension_sources`) + snapshot lignes/`updated_at` identique — avec méta-test de contrôle négatif prouvant que le filtre détecte bien une vraie requête, et preuve que le réconciliateur destructeur a réellement mordu sur le catalogue applicatif pendant la fenêtre observée _(Story 54.1)_
- [ ] [documentation](domains/documentation.md) — Site de documentation publique SE5 (socle) : build VitePress isolé dans `userDoc/` (package.json/lockfile dédiés, IBM Plex embarquée via `theme-without-fonts`, `base:/doc/`), page d'accueil à deux portes « J'administre SE5 » / « J'utilise mon poste » à sidebars keyées par chemin (isolation vérifiée), publication Apache verrouillée (Alias `/doc` → `userDoc/dist` seul, PHP off par répertoire, `-Indexes` off, pas de FallbackResource, `ErrorDocument 404` vers la page VitePress), sentinelle `update_apache()` étendue, `ensure_user_doc()` fail-soft dans `scripts/update.sh` (miroir-purge patron `ensure_ipxe_statics`, site précédent PROUVÉ intact par preuve checksum/mtime en cas d'échec de build/lien mort, page fantôme de sortie purgée), date de fraîcheur avec repli mtime (VM sans `.git`) _(Story 52.1)_

## À créer au fil des prochaines stories

- [x] `users.md` — créé story 14.4 (filtres audit). À compléter : création / modification / suppression / bulk reset MDP / itinérant
- [x] `parc.md` — créé Story 4.11 (appartenance unifiée pivot, salle physique, migration). À compléter : actions machine / batch groupes / schedules / wallpapers / AppProfiles
- [x] `ad-sync.md` — créé Story 4.9 (observer-driven Workstation, rename modrdn). À compléter : `/admin/sync-from-ad`, rapatriement profils LDAP
- [ ] `bootstrap-update.md` — `scripts/update.sh`, migrations, seed auto, cache reset
- [ ] `legacy-shims.md` — routes catchall legacy, modules GPO / iPXE / DHCP / BBB / imprimantes
- [ ] _(auth.md ajouté Story 16.10 — couvrira aussi `users.md` et `legacy auth/CAS/OAuth2` au fil des prochaines stories Phase 2/3)_

## Convention

Chaque domaine = un fichier Markdown dans `domains/`. Structure interne :

1. **Pré-requis communs** (VM, utilisateurs test, seed…)
2. **Sections par sous-domaine** (numérotation stable : `## Section 1 — …`)
3. **Scénarios numérotés** (`### Scénario 1.1 — …`) — **l'ID est stable**, ne pas renuméroter après ajout de nouveaux scénarios
4. **Post-correctifs & non-régressions** en fin de section (pourquoi ce scénario existe, quel incident il couvre)
5. **Checklist rapide** à la toute fin, cases à cocher pour le relecteur

Les corrections post-review ajoutent des scénarios dédiés (append), pas de réécriture des existants.

> **Format legacy** : les fichiers `{epic}-{story}-e2e-manual.md` (ex : `7-1-e2e-manual.md`) sont figés et ne servent plus de référence — ne pas en créer de nouveaux.

## Comment ajouter une checklist

1. Identifier le domaine principal de la story (Filesystem, Auth, Parc, etc.).
2. Si `docs/qa/domains/<domain>.md` existe : ouvrir et **append** une nouvelle section `## Story X.Y — <titre>` à la fin. Numéroter les scénarios à la suite (préserver les numéros existants — ils sont stables).
3. Sinon : créer le fichier en suivant la structure de `domains/rights-management.md`, et ajouter une ligne dans "Domaines couverts" ci-dessus.

## Pré-requis communs

Toutes les checklists supposent :

- VM accessible : `ssh -i ~/.ssh/id_se4fs_vm root@192.168.122.50`
- Migrations à jour : `cd /var/www/sambaedu-reload && php artisan migrate`
- Cache Spatie reset : `php artisan permission:cache-reset`
- User `admin` (ou équivalent SuperAdmin) pour les actions critiques (`server.admin`).

## Utilisation

Avant une mise en prod : ouvrir ce `README.md`, cocher les domaines impactés par le déploiement, puis dérouler chaque runbook domaine par domaine. Les scénarios doivent passer tous verts avant merge `main` → tag prod.

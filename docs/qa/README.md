# QA Manuel — Index pré-prod

Checklist par domaine métier. À dérouler avant une mise en production importante. Chaque fichier dans `domains/` est un runbook stable qui s'enrichit au fil des stories (pas de réécriture, append only).

## Domaines couverts

- [ ] [rights-management](domains/rights-management.md) — Droits applicatifs, rôles Spatie, permissions, délégations périmétrées, scoping classe, profils CRUD _(Stories 7.1, 7.2)_
- [ ] [filesystem](domains/filesystem.md) — Quotas, snapshot, trash, home dirs, nettoyage natif des profils itinérants (snapshot nocturne /home/profiles, détection + purge orphelins vers _Trash_users) _(Stories 5.1c, 5.1d, 5.2, 26.3)_
- [ ] [gpo](domains/gpo.md) — Group Policy Objects, profils itinérants, redirections, fondations natives `App\Gpo` _(Stories 1bis.18f, 16.1)_
- [ ] [wpkg-deploy](domains/wpkg-deploy.md) — Pipeline déploiement WPKG (logs, migrations, atomic write, chemins config) _(Story 15.1)_
- [ ] [network](domains/network.md) — Réservations DHCP, baux actifs, import CSV, migration legacy via `/sync-from-ad` _(Story 8.1)_
- [ ] [auth](domains/auth.md) — Plateforme HTTPS + JWT v1 poste↔serveur local (PKI locale, enrollment, refresh, révocation) _(Story 16.10)_
- [ ] [federated-login](domains/federated-login.md) — Login fédéré d'utilisateurs externes hors-AD (JWT signé RS256, IdP externe de confiance, anti-rejeu jti, mapping rôle, réconciliation guard) _(Story 20.1)_
- [ ] [ipxe](domains/ipxe.md) — Boot iPXE, résolution MAC/UUID, fondations Services Laravel _(Stories 3.1+)_
- [ ] [users](domains/users.md) — Listing /users, filtres audit (quota dépassé, mdp par défaut), synchro password_changed_at AD, pastille profil itinérant volumineux _(Stories 14.4, 26.3)_
- [ ] [ad-sync](domains/ad-sync.md) — Synchronisation AD observer-driven (Workstation, WorkstationGroup, …) + rename modrdn préservant objectGUID/netbootGUID _(Story 4.9)_
- [ ] [parc](domains/parc.md) — Appartenance poste↔groupe unifiée dans le pivot global, salles physiques (invariant 1-salle-max app-only), runbook migration/rollback, environnement de poste par parc (shared/personal/nomade, précédence multi-parcs, service de résolution Postgres-only) _(Stories 4.11, 26.1)_
- [ ] [agent](domains/agent.md) — Canal agent desired-state : ingestion des rapports POST /report, stockage D3 borné, agent squelette + compagnon de session, handlers wallpaper/overlay + route assets (démo palier 1), agent Go core/service SYSTEM/build signé, compagnon + handlers Go parité démo (binaire complet 2.1.x, zéro .ps1), conformité visible dans les pages parc (compteurs+badge+filtre, détail par type+événements, panneau groupe règles→exceptions) + forcer la synchro (PULL, bypass 304 / solde au report) — gate palier 1, distribution des releases serveur (binaires signés, manifest par ring = WorkstationGroup, canari/rollback), auto-update agent (double vérif SHA-256 + Authenticode, swap atomique anti-brique, report d'échec, version rapportée = preuve), enrôlement porte 2 des postes migrés (demande pending, approbation un-clic, mode campagne borné, anti-usurpation jamais débrayé — faisceau MAC/hostname/uuid), handler raccourcis (le bureau converge selon la nature du poste : chemin réseau/local résolu serveur = fix Bug C, union multi-mailles sans doublon, suppression level-triggered, première exposition du toggle strict/default rétroactif wallpaper/overlay), handlers lecteurs & imprimantes (l'imprimante de la salle = un item d'état comme les autres : provider `printers` défaut réglé par WG physique>logique + provider `drives` projection MVP-A des classes du user, handlers Go winspool/mpr natifs, union multi-mailles, suppression level-triggered, isolation serveur d'impression down → error, golden bumpé), rendu overlay VERROUILLÉ (Rainmeter portable posé par l'agent au bootstrap + écriture de `overlay.json` par le SERVICE SYSTEM au logon ACL `<SID>:R` infalsifiable + skin UTF-16 LE+BOM sans mojibake + `Rainmeter.ini` durci ACL ProgramData + watchdog compagnon, pas d'obfuscation), drift policy PAR ASSIGNATION (le mode strict/default suit la cible et plus la règle : un même raccourci verrouillé sur un parc, modifiable sur un autre — `mode` déplacé `shortcuts` → pivot `shortcut_assignables`, wallpaper/overlay déjà « par cible », toggle au geste d'assignation, contrat agent + golden INTACTS car mode jamais émis au payload), icône UPLOADÉE → asset statique servi par Apache (l'icône uploadée d'un raccourci s'affiche enfin réellement, plus de « feuille blanche » : `.ico` content-addressed `<sha>.ico` servi en DIRECT par un Alias Apache scopé — garde-fou sécurité : JAMAIS `storage/keys/pki/` — provider distingue nom-nu/chemin-réel + émet `{icon_asset, icon_checksum}` SANS url, agent fait un GET HTTP simple sans token + vérifie le SHA-256 avant écriture locale + pointe l'IconLocation dessus, convergence gracieuse si asset indisponible, backfill artisan name→content-addressed, golden bumpé sciemment PHP↔Go), catalogue de tools agent (le portable Rainmeter ET la skin deviennent des assets gérés serveur, toggleables depuis l'UI : upload validé du `.zip` portable — structure ZIP `Rainmeter.exe`+`Skins/`, SHA-256 calculé serveur, filename dérivé anti-traversal, mono-version — + manifest dédié `tools-manifest` hors items desired-state + skin SERVIE par la route agent authentifiée — l'embed `go:embed` retiré, plus de recompilation à la retouche — checksum du portable lu de l'état, toggle global no-op à la désactivation SANS désinstaller, golden overlay INTOUCHÉ), retrait du mode strict/default — STRICT partout (DÉMONTAGE du mécanisme `mode` strict/default introduit par 27.1 et déplacé par 27.3 : la review 27.3 a montré que le grain réel du mode était `type × poste` pas `item × cible` → promesse creuse ; comportement cible UNIQUE = la cible fait TOUJOURS loi, la dérive humaine est toujours corrigée, `drifted_allowed` disparaît ; profondeur schéma→agent : item du contrat 5 clés → 4, statut report 4 → 3, `FROZEN_STATE_HASH` PHP+Go bumpés croisés, colonnes `mode` des 3 tables droppées réversibles, enum `StateMode`/`StateCandidate::$mode`/`StateProvider::mode()`/`StateCompiler::aggregateMode()` supprimés, 3 toggles UI retirés, agent Go `ResolveItemStatus(isCompliant)` sans mode — `AppliedState`/`AggregateHash`/isolation conservés ; abroge les scénarios 4.4/6.6/13.5/Section 16, ajoute Section 19), réveil de l'agent au logon — cycle desired-state (RUNTIME/boucle agent, PAS un handler : au `WTS_SESSION_LOGON` la boucle Go part sur un cycle complet immédiat au lieu d'attendre le tick ~1 h/TTL 24 h — canal `wake` bufferisé 1 + `RequestWake()` non-bloquant coalescé, debounce min-interval 60 s côté boucle, send hors du recover overlay 27.1bis = réveil indépendant, canal nil-safe inerte hors Windows ; zéro impact serveur SE5/contrat/golden/migration, go.mod inchangé ; Section 20), handler registre par parc (CATALOGUE de réglages registre Windows prédéterminés activables par parc — l'admin non-expert ne tape jamais un chemin `HKLM\…` : table dédiée `registry_settings` + pivot `registry_setting_assignables` calqué shortcuts ; le réglage se COMPILE en item CONCRET `{hive,path,name,type,value}`, l'id de catalogue ne fuite JAMAIS au payload = couture pour brancher les clés brutes en v2 sans coût ; DEUX providers serveur HKLM/machine + HKCU/session mais UN handler Go générique idempotent — premier type MACHINE du canal : le service SYSTEM gagne son moteur de convergence machine ; exclusive PAR IDENTITÉ DE CLÉ `{hive,path,name}` au compilateur via marqueur `KeyedExclusiveProvider` ; INVERSION GLOBALE de précédence `logique > physique` D-Q3 propagée à wallpaper/printers ; fusion par type avant report car les 2 portées émettent `registry` ; drift STRICT réimpose, désactiver = cesser de gérer ; golden +1 item registry, `FROZEN_STATE_HASH` PHP+Go bumpé croisé 1599cc48…→2b49f008… ; catalogue initial 3 réglages HideFileExt/Hidden/EnableLUA ; Story 27.3), associations de fichiers par parc (type `associations` — programmes par défaut `.pdf`→Acrobat/`http`→Firefox appliqués au logon par le compagnon sous HKCU UserChoice : table dédiée `file_associations` + pivot `file_association_assignables` ; le réglage se COMPILE en item CONCRET `{identifier,progid,type}`, ni id de catalogue ni hash/SID au payload ; CŒUR DE RISQUE = le hash anti-tamper UserChoice porté FIDÈLEMENT de `SFTA.ps1::Get-Hash` en Go (~50 lignes : MD5 sur UTF-16LE + 2 passes de dérivation à constantes + XOR fold Base64) calculé 100% AGENT-side car il dépend du SID/FileTime/GUID experience du poste — VERROUILLÉ par tests vectoriels contre un portage indépendant ; exclusive PAR IDENTIFIANT au compilateur (`KeyedExclusiveProvider`, zéro modif compilateur), `logique > physique` D-Q3 ; ProgId cible NON installé → l'agent NE clobbe PAS la clé UserChoice existante = choix utilisateur PRÉSERVÉ, `error` NON fatal isolé, pas de réécriture en boucle ; suppression-avant-réécriture de la clé à ACL hérité ; seeder de REPRODUCTION legacy `FileAssociationSeeder` parse `default.xml` si présent sinon baseline figée FirefoxHTML/FirefoxURL/visionneuse ; drift STRICT réimpose, désactiver = cesser de gérer ; golden +1 item associations `FROZEN_STATE_HASH` PHP+Go bumpé croisé →77fb548a… ; EXTENSION WPKG-aware D-Henri n°7 : catalogue tagué `native` vs `wpkg`+`wpkg_package` (SERVEUR-only, jamais au payload) + validation PRÉDICTIVE UI par parc — `native`→applicable, `wpkg` déployé→applicable, `wpkg` non déployé→indisponible (warning EXACT + toast nommant le paquet AVANT déploiement, remplace le warning générique) ; paquets déployés du parc = group-level Eloquent PG-pur SANS cache APCu, jointure `<package id>`=`Application::app_id` VÉRIFIÉE ; provider INCHANGÉ (PG-pur émet toujours, NFR7), golden/hash/agent INTOUCHÉS ; Story 27.3bis), registre — valeur par défaut diffusée + override par parc (ÉVOLUTION serveur+UI de 27.3, PAS un nouveau type : `registry_settings.value` devient la VALEUR PAR DÉFAUT diffusée à TOUTE la flotte via la maille Broadcast — le provider émet désormais un candidat Broadcast par réglage actif de la ruche EN PLUS des candidats par maille — et `registry_setting_assignables.value` est un OVERRIDE de valeur par parc, repli sur le défaut si null ; l'override bat le défaut via la précédence logique>physique>broadcast EXISTANTE, exclusive par clé et StateCompiler INCHANGÉS ; PAS d'opt-out/tombstone — « retirer un override » = REVENIR AU DÉFAUT, l'agent re-converge — D3 ; posture par défaut SÛRE D6 : `EnableLUA` défaut 0→1 UAC ACTIVÉ, désactivation = override de parc délibéré ; warning au déclenchement D7 = colonne `registry_settings.warning` + `options` [{value,label}] pour le contrôle UI à choix fermé, NI l'un NI l'autre au payload ; DEUX surfaces UI : onglet « Registre » du parc REFONDU toggle→overrides seulement (ajouter/éditer/retirer + contrôle adapté au type + validation serveur + confirmation warning) + NOUVELLE page réglages serveur `/admin/settings/registry` éditant le DÉFAUT du catalogue ; CONTRAT & AGENT INCHANGÉS, golden/hash INTACTS car `state.v1.json` est hand-authored — jamais compilé d'un seed — donc la diffusion Broadcast ne le touche pas, ZÉRO fichier Go modifié ; canal legacy Registry.pol/GPO intouché meurt en 27.6 ; Story 27.3ter), config d'app déclarative (type `app_config` — policies de navigateur Firefox/Thunderbird via le SEUL mécanisme enterprise natif `policies.json` posé au chemin d'install par le moteur SYSTEM (correctif post-review #1 : `policies.json` machine-wide admin-write, scope `machine` — un compagnon user prendrait ACCESS_DENIED) : PAS de nouvelle table, on LIT la table métier `app_customizations` (4.8) via `AppCustomizationService::resolvePoliciesForMachine($wg, null, …)` PG+config-pur NFR7 — résolution PAR PARC niveaux 1-4 template/auto/défaut/WG ($user=null : le par-user de Firefox = le profil/Mécanisme B/roaming hors 27.4, PAS policies.json) ; aggregate PAR `app_kind` un item par app, scope `machine` ; payload `{app_kind, policies}` CONCRET jamais un id de scope, sans float §4.1 ; UN seul mécanisme — écriture atomique du `policies.json` (canonicalisation UNIFIÉE `shared.CanonicalJSON` cible↔fichier relu, idempotence json.Number — review #3), marqueur de périmètre `_sambaedu_managed` = fichier posé hors SambaEdu jamais écrasé/supprimé MAIS conflit rapporté `error` jamais `compliant` trompeur (review #7) ; level-triggered = policy retirée dé-appliquée, drift STRICT réimpose à l'octet près ; isolation chemin verrouillé/app absente → `error` isolé les autres types continuent, couplage install 27.5 = limite connue ; tiebreak multi-parcs logiques = plus petit id gagne, 2ᵉ parc ignoré (limite connue review #2) ; APP BUTÉE sans mécanisme enterprise = NON géré documenté zéro bricolage ; RECADRAGE 2026-06-17 Chrome/Edge + redirection de profil RETIRÉS (roaming → 26.x) ; AUCUNE nouvelle UI édition policies 4.8 inchangée ; golden +1 item app_config en portée machine `FROZEN_STATE_HASH` PHP+Go bumpé croisé 77fb548a…→6f0ff33e… ; Story 27.4) _(Stories 24.1 → 25.3, 27.1, 27.2, 27.1bis, 27.3, 27.3bis, 27.3ter, 27.4, 27.7, 25.6, 27.8, 27.9)_

## À créer au fil des prochaines stories

- [x] `users.md` — créé story 14.4 (filtres audit). À compléter : création / modification / suppression / bulk reset MDP / itinérant
- [x] `parc.md` — créé Story 4.11 (appartenance unifiée pivot, salle physique, migration). À compléter : actions machine / batch groupes / schedules / wallpapers / AppProfiles
- [x] `ad-sync.md` — créé Story 4.9 (observer-driven Workstation, rename modrdn). À compléter : `/admin/sync-from-ad`, rapatriement profils LDAP
- [ ] `bootstrap-update.md` — `scripts/update.sh`, migrations, seed auto, cache reset
- [ ] `legacy-shims.md` — routes catchall legacy, modules GPO / iPXE / DHCP / BBB / imprimantes
- [ ] _(auth.md ajouté Story 16.10 — couvrira aussi `users.md` et `legacy auth/CAS/OAuth2` au fil des prochaines stories Phase 2/3)_

## Convention

Chaque domaine = un fichier Markdown dans `domains/`. Structure interne :

1. **Pré-requis communs** (VM, utilisateurs test, seed…)
2. **Sections par sous-domaine** (numérotation stable : `## Section 1 — …`)
3. **Scénarios numérotés** (`### Scénario 1.1 — …`) — **l'ID est stable**, ne pas renuméroter après ajout de nouveaux scénarios
4. **Post-correctifs & non-régressions** en fin de section (pourquoi ce scénario existe, quel incident il couvre)
5. **Checklist rapide** à la toute fin, cases à cocher pour le relecteur

Les corrections post-review ajoutent des scénarios dédiés (append), pas de réécriture des existants.

> **Format legacy** : les fichiers `{epic}-{story}-e2e-manual.md` (ex : `7-1-e2e-manual.md`) sont figés et ne servent plus de référence — ne pas en créer de nouveaux.

## Comment ajouter une checklist

1. Identifier le domaine principal de la story (Filesystem, Auth, Parc, etc.).
2. Si `docs/qa/domains/<domain>.md` existe : ouvrir et **append** une nouvelle section `## Story X.Y — <titre>` à la fin. Numéroter les scénarios à la suite (préserver les numéros existants — ils sont stables).
3. Sinon : créer le fichier en suivant la structure de `domains/rights-management.md`, et ajouter une ligne dans "Domaines couverts" ci-dessus.

## Pré-requis communs

Toutes les checklists supposent :

- VM accessible : `ssh -i ~/.ssh/id_se4fs_vm root@192.168.122.50`
- Migrations à jour : `cd /var/www/sambaedu-reload && php artisan migrate`
- Cache Spatie reset : `php artisan permission:cache-reset`
- User `admin` (ou équivalent SuperAdmin) pour les actions critiques (`server.admin`).

## Utilisation

Avant une mise en prod : ouvrir ce `README.md`, cocher les domaines impactés par le déploiement, puis dérouler chaque runbook domaine par domaine. Les scénarios doivent passer tous verts avant merge `main` → tag prod.
